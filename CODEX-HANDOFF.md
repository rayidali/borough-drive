# Borough Drive: continuation context

## Current checkpoint — September 23, 2026: browser-reference refinement complete locally

The user requested redoing **both sides of East 7th between First Avenue and Avenue A** through ordinary browser Street View observation, including each property's visible address placement. Revision 07 is implemented, exported and reviewed locally. [Report and game images](model-source/SEVENTH-ENGINE-07.md) · [validation index](model-source/east-seventh-review-07/validation.json). No publication was requested.

- **Source/save:** branch `feature/east-seventh-fidelity-06`; baseline source/export `acf1d18fe36879faf74d1589e25c7330bbe4ef48`, prior checkpoint `f176aed`. Implementation, source, export, evidence and reconstruction guidance are saved locally in **`098f1cc89b41c2067fe1577ddb62df5a3b3c6002`**. This checkpoint follows in a separate documentation commit identified through Git history. No implementation or export remains pending; neither local save is pushed.
- **Reference/implementation:** all 37 frontage records plus the 108 site were inspected in ordinary browser Street View, using April 2026 imagery. All 35 editable building models changed; the two accepted First Avenue records remain protected. Individual doors, number placements, upper windows, shop divisions, stoops/rails/oriels, church details, dated sheds/scaffolds and Avenue A returns are refined. The 108 gated/planted street edge is modeled; hidden rear-building dimensions are unresolved. [Source ledger](model-source/east-seventh-browser-07.json) records factual observations and occlusions. Zero paid API image requests in this pass; no billing/credential changes. Source images are temporary previews, not committed assets or textures.
- **Preservation:** all 147 protected/out-of-scope GLB hashes are identical to baseline. Original-game files and non-Seventh storefront data, shared legacy storefront kit, west block, accepted corner, compact UI, controls/camera source and audio are preserved. Two private screenshots are unchanged/untracked/excluded. [Model hashes](model-source/east-seventh-review-07/model-preservation.json) · [other preservation](model-source/east-seventh-review-07/preservation.json).
- **Final package:** Godot 4.7.2; PCK `746113ee606ebfb6b6b0e32861974ce2ae3445c25d7a6b509b011c7c6bd393fa`, **89,466,244 bytes**. Fourteen runtime files total **129,835,962 bytes**, 86 matching source hashes. `build.json.sourceCommit` remains an inherited geographic baseline, not this task's actual Git save. Building triangles **3,401,466 → 3,466,757** (+1.92%). Keep `dist/index.html` absent; local `/` and `/seventh/` match Seventh, `/index.html` matches legacy.
- **Actual review/checks:** all 37 final elevations, 52 overlapping ground images and nine obliques were visually inspected. Original/Seventh package and architecture verification pass. Native address (76 frontages), handling and four-mode camera tests pass; their logs retain the macOS certificate-enumeration warning. Seventeen browser address cases and four camera inspection modes pass. Final-package driving/braking/drift, six full-block routes in three weather states (zero contacts), pause/reset/steering pass. A focused repeat passes settings reload, Retina sizing, true tab-switch pause, postcards and missing-pack/graphics-context recovery.
- **Failures/test boundary:** the first post-batch full run timed out during settings reload after the driving checks passed. The document was visible but OS-unfocused; the engine eventually became ready while the shell retained its watchdog error. The harness now holds focus during ordinary work and disables emulation for its explicit focus-loss test; `--recovery-only` repeats independent remaining checks. The failed report and passing focused repeat are retained. Native/address/camera-inspection checks used pre-batch PCK `362be9c…afbf1`; all 182 GLBs and the other 85 package-source hashes are identical after the 108 static-batching-only correction. Final captures, performance, driving and focused recovery use `746113e…393fa`. [Exact boundaries](model-source/east-seventh-review-07/batching-boundary.json). No runtime loader change was made.
- **Performance:** this session's unchanged baseline **40.13–53.93 FPS**, final **37.68–55.54 FPS**; nine-sample means **46.17 → 46.64 FPS**, local ready **8.833 → 8.965 s**. Apple M1/8 GiB, Brave Chromium 153, 1440×1000/DPR 1, final scales 0.75–1. The new 108 group initially bypassed existing batching; its static meshes now share the existing batches, reducing westbound sample draw calls from **813–818 to 621–626** without deleting detail. The **60 FPS target is not demonstrated**; do not reuse the previous session's ~60 FPS numbers or claim a statistically established speedup. [Measurements](model-source/east-seventh-review-07/performance-comparison.json).
- **Remote/deployment:** no fetch, push, deployment or live recheck. The earlier September 21 interface/revision 04 publication remains the last verified live baseline; this local revision has not replaced it.
- **Accuracy limits:** April imagery does not establish September conditions. Occluded/unreadable numbers, tiny lettering, exact ornament/curved ironwork, measured dimensions and hidden surfaces remain unresolved. Curb-tree/lamp/hydrant/furniture positions remain the previous estimates outside 108 and facade-local sheds. **Zero calibrated multi-view passes**; no photographic 1:1 certification. The source/render distinction remains explicit.

**Next action:** user review at **http://127.0.0.1:5173/** (`npm run dev` if needed), driving east from First Avenue toward Avenue A. All player assets, recipes, dated URLs/observations, final game images and review evidence are saved in the repository; no temporary browser profile, old process or API key is needed to resume. The reconstruction skill now records effective ground overrides, source-grounded count corrections, corner address ownership, protected legacy recipes, and direct-child runtime batching. Do not automatically expand to another block, retry paid references, change UI/audio, or publish from this handoff.

## Previous checkpoint — September 21, 2026: First Avenue–Avenue A public-reference pass saved locally

Recorded **2026-09-22T03:08:17.395951+00:00** (September 21, America/New_York). The user requested implementing **both sides of East 7th Street between First Avenue and Avenue A** to the endorsed First–Second visual standard. Revision 06 is implemented as a locally reviewable public-reference candidate. Publication is not requested. [Report and actual game views](model-source/SEVENTH-ENGINE-06.md) · [validation index](model-source/east-seventh-review-06/validation.json).

- **Source/save:** baseline `f2bf1bf486ff1db0f28c3bc28be5c3aca5ad6a42`; branch `feature/east-seventh-fidelity-06`. Implementation/source/export/evidence are saved locally in `acf1d18fe36879faf74d1589e25c7330bbe4ef48`. This checkpoint documentation follows in its own commit; use Git history for that commit, rather than amending it to contain itself. No work is pushed or deployed in this task. Cached `origin/main` and the September 21 publication evidence are historical; remote/live state was not rechecked.
- **Complete bounded implementation:** 37 Seventh-facing records (35 editable, two protected corner records), 33 changed building models, two retained editable models. Individual bay/floor/entry/stoop/cornice corrections, St Stanislaus and former St Mary profiles, McKinley, Mary O's and Avenue A shop returns; engine-only 109/116 and range identity corrections; address HUD through Avenue A. [Inventory](model-source/east-seventh-coverage-06.json), [observations](model-source/east-seventh-observations-06.json), [review ledger](model-source/east-seventh-review-06/visual-review.json).
- **Preserved:** all 147 out-of-scope/protected building hashes, original game/geography, accepted First Avenue corner, west block, compact UI/car/camera source and audio. Total 149 unchanged / 33 changed model hashes. Both private screenshots remain unchanged/untracked/excluded. [Preservation](model-source/east-seventh-review-06/preservation.json).
- **Final package:** PCK `600c609cfb9bb17267e0668bcf9729c46ef0db2fd4f442758461688c0aa56432`, **88,153,136 bytes**; 14 runtime files total **128,522,854 bytes**, 84 matching source hashes. Godot 4.7.2. `build.json.sourceCommit` is the inherited geographic baseline, not this source save. `dist/index.html` remains absent; `/` and `/seventh/` use Seventh, `/index.html` remains legacy.
- **Actual checks:** original and Seventh verification pass; native address (76 frontages), handling and cameras pass; 17 browser address cases and four-POV inspection checks pass. Full browser driving/braking/drift, six complete east-block routes in three weather states with zero contacts, pause/tab switching, settings/Retina, postcards and missing-pack/context recovery pass with zero unexpected runtime errors/failed requests. 89 final elevation/ground images plus four obliques reviewed. Earlier failed/interrupted attempts remain recorded, not relabeled as passes.
- **Exact review boundary:** visual/camera/full-behavior checks used PCK `c29296a…b3cc9`. The final metadata refresh corrected explanatory counts and a partial-export review-height bug; all rendered model/collision records and runtime code remain identical. Full and partial exports now produce identical manifests. Final package verification, nine performance samples and 17 address cases use PCK `600c609…56432`. [Proof and hashes](model-source/east-seventh-review-06/metadata-refresh.json).
- **Performance:** same-environment baseline **59.00–60.04 FPS**, final **59.75–60.03 FPS**, final scale 1 in all nine samples; local ready **5.873 → 5.579 s**. Apple M1 / 8 GiB, Brave Chromium 153, 1440×1000 / DPR 1, short local tests. Building triangles **3,362,194 → 3,401,466** (+1.17%); PCK +0.87%. No sustained/mobile/capacity or speedup claim. [Comparison](model-source/east-seventh-review-06/performance-comparison.json).
- **Reference decision:** user authorized up to 40 Street View images. Twelve metadata requests were denied for disabled billing; **zero image requests**. User then chose **“Finish using the public references for now.”** No retry is scheduled; no credentials/account settings changed. Architectural archive dates are often unknown; dated storefront observations/current official identities stay separate. No downloaded source pixels enter the game or committed images.
- **Remaining accuracy work:** current 108 site, exact tree/lamp/hydrant/curb-furniture and temporary construction placement, occluded/latest tenant details, hidden surfaces, measurements and calibrated source-camera comparison. The current upper elevation of 131 was not re-established; its model is retained. **Zero calibrated multi-view passes**, and no claim that the full block has reached photographic 1:1 acceptance. Street fixtures remain the existing runtime treatment, distinct from corrected facade-local stairs/rails.

**Next action:** review the local candidate with the user: `npm run dev`, **http://127.0.0.1:5173/**, drive east along Seventh toward Avenue A. Read the report/coverage ledger before a specific refinement. The updated repository skill records effective ground overrides, corner-face ownership and partial-export checks for later lower-effort work. No rebuild, API key, old browser process or temporary reference folder is needed to play/resume. Do not automatically retry Street View, expand to another block, change audio/UI, or publish from this handoff.

## Previous checkpoint — September 21, 2026: session closed with resumable documentation

Recorded **2026-09-21T21:18:39-04:00**. The user requested all relevant Markdown be updated before closing the session. This follow-up changes documentation only. The compact interface is already published; no next implementation task or background reconstruction job is assigned.

### Exact saved and published state

| Item | State to resume from |
| --- | --- |
| Branch at start of closing update | `main` and cached `origin/main` both `533570ed9ee458e7f5104c23e35d6b19447d7122`; only two private screenshots were untracked |
| Interface source/runtime | `b194dc6ca371b588b27f073bd6cf5be9df4fa9ca` — compact launch/HUD/pause/loading, Inter, supplied logo, narrow layouts and review evidence; pushed |
| Publication checkpoint | `533570ed9ee458e7f5104c23e35d6b19447d7122` — pushed; [CI success](https://github.com/rayidali/borough-drive/actions/runs/35671744723), both Node.js 22 and 24 passed; [Vercel success](https://vercel.com/rayidalis-projects/borough-drive/4ssCMoWSCSJaTwnvZa2DCoQ6TkuA) |
| Architectural source | `d4fbafe9a6af4497de81e2220d31c878f930612e`, revision 04; all 182 building assets, geographic data, original game and audio preserved by the interface pass |
| Routing baseline | `bfbc8d764f838708f753a53de9b7f054101b45f0`; `/` and `/seventh/` serve Seventh, `/index.html` serves `dist/legacy.html`; keep `dist/index.html` absent |
| Final package | PCK SHA256 `e091db30994a4fbe2d42cec5d190b727d17de8959dadc52df744153da8d48131`, 87,390,432 bytes; all 14 runtime files total 127,760,150 bytes; 82 source hashes in [build.json](dist/seventh/build.json) |

The last publication task verified both **https://www.drivearound.nyc/** and **https://www.drivearound.nyc/index.html** against the final shells after `533570e`; `https://drivearound.nyc/` redirects to the branded www host. The Vercel host/direct route, manifest, logo, font, license and credits also matched. No DNS changes were needed. The large live PCK download timed out; total-size and 65,536-byte prefix checks passed, so do not claim a full remote PCK hash. [Publication evidence](model-source/interface-review-2026-09-21/publication.json) records the implementation-commit checks; the later checkpoint-commit CI/deployment links are above. This closing task does not rerun the game or establish a new performance/fidelity result.

This closing Markdown is saved as a subsequent documentation commit under the preceding request to push the work to main. Find its own hash/status with `git log --oneline -- CODEX-HANDOFF.md SESSION-LOG.md` and `git status --short --branch`; do not repeatedly amend the checkpoint to insert its own hash. Neither private `Screenshot 2026-09-14…png` nor `Screenshot 2026-09-21…png` belongs in a commit. Preserve both original local files.

### What is complete and what remains

- **Complete:** portable [Seventh reconstruction skill](model-source/skills/reconstruct-street/SKILL.md), source/build adapter, detailed quality standard and small worker/reviewer packets; published [interface pass and styling contract](model-source/INTERFACE-PASS-05.md); original logo/font sources and licenses; actual review images and package records. No global skill installation or automated worker service is required to read the Markdown.
- **Verified locally during implementation:** final layout/start/pause/help at desktop/laptop/narrow/Retina sizes; final four-POV camera inspection and nine address cases; structural/package checks. The broader driving/settings/postcard/recovery suite used the preceding UI package before the final narrow-layout adjustment. Preserve the [exact test boundaries](model-source/interface-review-2026-09-21/validation.json), including the successful repeats after review-tab/transport stalls.
- **Remaining experience work:** more audible, less repetitive and calming music with actual listening/mix review; dedicated tiny-icon/social artwork and legacy branding if requested; further aesthetic feedback. The [experience brief](docs/DRIVEAROUND-EXPERIENCE-BRIEF.md) now distinguishes completed requirements from these proposals. Audio remains the original 12-second music loop at −23 dB; no new listening acceptance was recorded.
- **Remaining reconstruction work:** no next street is assigned. Revision 04 is the user-endorsed visual reference, with 39 source/elevation reviews and zero calibrated multi-view passes. No new 1:1 certification, cheaper-model cost/quality qualification or unattended acquisition/build pipeline exists. The [benchmark](model-source/SEVENTH-FIDELITY-BENCHMARK.md) retains its proposed photographic thresholds; published UI does not change them.
- **Closing documentation:** current instructions, entry-point READMEs, contribution/resume guidance, experience brief, interface report, reconstruction skill references and historical engine/benchmark headers were reconciled. Earlier dated evidence and all license notices remain intact. Validation passed: all 430 relative Markdown links across the 17 changed documents resolve, the skill validator passes, whitespace checks are clean, and non-Markdown files are unchanged. No rebuild is required.

### Next session

1. Read this checkpoint, the latest [session log](SESSION-LOG.md), [AGENTS.md](AGENTS.md), [start instructions](START-HERE.md) and [neighborhood notes](model-source/NEIGHBORHOOD-NOTES.md). Inspect branch/history/status and preserve private or uncommitted work.
2. To review the game, run `npm run dev` and open **http://127.0.0.1:5173/**. Existing tracked exports are sufficient. No Godot/Blender rebuild, new install, temporary image folder, old browser tab or surviving server process is needed to resume.
3. Follow the user's next bounded task. For a named street, use the skill and specify endpoints/sides/corner extent before implementation. For music/branding, use the experience brief and preserve the published UI. Do not automatically execute a historical plan or the unfinished fidelity backlog.

Copyable opener: **“Read AGENTS.md and the current CODEX-HANDOFF.md, inspect Git status, then help with [my next bounded task]. Preserve the published interface and architectural baseline.”**

## Previous checkpoint — September 21, 2026: compact interface published and verified

Recorded **2026-09-21T20:22:43-04:00**. The user explicitly requested **“push it to main”** after the finished interface review. The redesigned UI is pushed and deployed. [Play DriveAround.nyc](https://www.drivearound.nyc/) · [Interface report](model-source/INTERFACE-PASS-05.md) · [Publication evidence](model-source/interface-review-2026-09-21/publication.json).

- **Published source/runtime:** `b194dc6ca371b588b27f073bd6cf5be9df4fa9ca` on `main`/`origin/main`, containing the compact launch/HUD/pause/loading design, Inter and supplied logo, exported package, review evidence and documentation. Fetched origin and confirmed no divergence before pushing `d754647..b194dc6`. This publication checkpoint follows in a separate documentation commit; find its own revision in Git history rather than amending it to contain itself.
- **CI:** [Verify run 35659130950](https://github.com/rayidali/borough-drive/actions/runs/35659130950) completed successfully for `b194dc6`; Node.js 22 and Node.js 24 both passed. Local `npm run seventh:verify`, `npm run verify` and whitespace checks passed again before publication. Independent publication audit found no blocker.
- **Deployment:** [Vercel reports success](https://vercel.com/rayidalis-projects/borough-drive/13kgVBv3gjEHEH1k63NzMLV5c5Ax). Live HTTPS `/`, `/seventh/`, `/index.html`, build manifest, font, logo, font license and credits exactly match disk. `https://drivearound.nyc/` redirects to `https://www.drivearound.nyc/`, whose root exactly matches the new shell. No DNS or domain configuration was changed.
- **Pack evidence:** local committed PCK SHA256 remains `e091db30994a4fbe2d42cec5d190b727d17de8959dadc52df744153da8d48131`, **87,390,432 bytes**. Live range request returned 206, the same total size and an exact 65,536-byte prefix match. A full live download timed out after 42,790,366 bytes; no full live PCK hash or new public-browser performance result is claimed. The complete local package/source checks and preceding browser evidence retain their recorded scope.
- **Preservation:** architecture/gameplay-model baseline `d4fbafe9a6af4497de81e2220d31c878f930612e`, routing `bfbc8d764f838708f753a53de9b7f054101b45f0`, models/audio/original game remain unchanged. `dist/index.html` stays absent. Both private screenshots remain unchanged/untracked and were excluded from the commit. All original logo and license/provenance files are preserved.
- **Remaining work:** none for this interface publication. Music loudness/arrangement, dedicated small-icon artwork, legacy branding, further aesthetic review and future streets remain separate tasks. No additional modeling, source acquisition, audio job or paid/background pipeline was started.

**Next action:** continue from the user's next bounded request. The UI is on main and live; do not automatically start the remaining experience or reconstruction backlog.

## Previous checkpoint — September 21, 2026: compact interface ready for local review

Recorded **2026-09-21T17:23:00-04:00**. The user rejected the oversized left intro, fonts and layout and requested professional product polish with an indie-game lo-fi mood. The bounded local interface redesign is complete. [Report, styling contract and screenshots](model-source/INTERFACE-PASS-05.md).

- **Play:** `npm run dev`, then **http://127.0.0.1:5173/**; `/seventh/` is the same export. `/index.html` remains the original ten-block game. Existing tracked `dist/seventh/` includes the finished UI; no rebuilding is needed to review it.
- **Implemented:** compact bottom launch panel, restrained identity/weather/sound controls, smaller instrument panel, coherent pause menu with focusable Controls disclosure, quiet loading/retry presentation, local Inter 4.1 and the unchanged supplied logo in HUD/loading/tab icon. Native layout stacks below 680 px; speed/address/actions remain while the small map drawing yields room. No new models, audio, world lighting or driving behavior.
- **Exact save:** current `main` and cached `origin/main` remain `d7546478a101d10e15c8a6f1ef46ed6c850dc052`. UI source/assets/export, two review-runner readiness fixes, evidence and Markdown are **local and uncommitted**. Gameplay/model baseline remains `d4fbafe9a6af4497de81e2220d31c878f930612e`; routing baseline `bfbc8d764f838708f753a53de9b7f054101b45f0`. Both private screenshots remain unchanged/untracked; exclude them from any later commit.
- **Final package:** built `2026-09-21T21:14:23.340Z`, Godot 4.7.2; PCK SHA256 `e091db30994a4fbe2d42cec5d190b727d17de8959dadc52df744153da8d48131`, **87,390,432 bytes**. Fourteen runtime files total **127,760,150 bytes**; all 82 source hashes match. [Manifest](dist/seventh/build.json), [final interface record](model-source/interface-review-2026-09-21/interface-review.json). `build.json.sourceCommit` remains the inherited geographic baseline, not checkout HEAD.
- **Validation:** clean final native export and `npm run seventh:verify`; original `npm run verify` passed. Final UI reviewed at 1440×1000, 800×600, 640×600, 390×844 and 1100×800/DPR 2, with start/driving/pause/help, keyboard focus and zero recorded runtime errors. Final focused camera checks passed all four POVs, orbit/pan/zoom/recenter, stable car transform, HUD release and paused input; all nine address-scope cases passed. [Validation index](model-source/interface-review-2026-09-21/validation.json).
- **Broader evidence:** the full browser suite passed driving/brake/reverse/drift, 12 weather/camera combinations, settings reload, tab-loss pause, Retina cap, postcard and both failure/retry paths on the immediately preceding package, before the final narrow-layout adjustment. Its exact build is retained separately. The final camera/address checks encountered one input transport timeout and one hidden-tab startup timeout; fresh/foregrounded repeats passed. Review runners now foreground the tab and the camera runner waits for the loading overlay to hide. These are recorded, not omitted from the evidence.
- **Preservation/routing:** all 182 GLB hashes, model recipes/geography, controller/camera/audio sources, original game assets and Vercel rules unchanged. All seven checked local route/brand/font/license/credit URLs match disk; `dist/index.html` remains absent. Original logo bytes and all notices are preserved. [Preservation](model-source/interface-review-2026-09-21/preservation.json), [local URLs](model-source/interface-review-2026-09-21/local-routes.json).
- **Publication:** no commit, fetch, push, deployment or DNS action for this UI follow-up. The earlier documentation publication remains on main; its live verification is the preceding checkpoint. No unfinished build, model or background reconstruction job.
- **Limits:** no new photographic or listening acceptance. Music remains unchanged; dedicated small-icon artwork/social images/legacy branding remain in the experience brief. The full browser run measured 8.23 s startup and 39.1–53.8 FPS locally; this was not a controlled before/after performance comparison. Desktop keyboard/mouse remains the tested control scheme; narrow layout is not touch-driving or full accessibility support.

**Next action:** review the local interface with the user and follow their next bounded direction. Keep the prepared reconstruction skill and architecture revision 04 intact. No automatic street expansion, music rewrite or publication follows from this UI pass.

## Previous checkpoint — September 21, 2026: reconstruction guidance pushed to main

Recorded **2026-09-21T16:30:47-04:00** (America/New_York). The user explicitly requested **“push it to main”** after the documentation-only work below. Publication of the prepared guidance is complete; implementation remains deferred.

- **Published save:** `180ebccc8e06c030388c0e662cdc37fd93bc3ad6` on `main`/`origin/main`, containing 15 Markdown files and the original unchanged logo source asset. Includes the [portable skill](model-source/skills/reconstruct-street/SKILL.md), [experience brief](docs/DRIVEAROUND-EXPERIENCE-BRIEF.md), linked instructions and provenance corrections. The logo is tracked so a fresh checkout retains the brief's source image. Private screenshot remains untracked/excluded and unchanged.
- **Gameplay/source baseline:** unchanged `d4fbafe9a6af4497de81e2220d31c878f930612e`; runtime/routing baseline `bfbc8d764f838708f753a53de9b7f054101b45f0`. No `dist/`, model, runtime script, audio or exported game byte changed. Saving the source logo does not integrate it into the website.
- **Git:** fetched the existing origin and confirmed no divergence before committing; push `e65e835..180ebcc` succeeded. This publication checkpoint follows in a separate documentation commit; obtain its own hash and push state from Git history/status rather than amending it to contain itself.
- **Checks:** skill validator and committed whitespace checks pass. Independent publication audit found no broken tracked link or credential-pattern match. Logo SHA256 remains `9156dec61cc0ab10adb5b471e7f80d77d4e79102c2a4069e09227f93462f5448`; private screenshot remains `a182ce14205d2c4d6125f5f332b80ff643b77b56f0117678e7a2d83541fd652d`. GitHub [verification run](https://github.com/rayidali/borough-drive/actions/runs/35651413773) for `180ebcc`: Node.js 22 and Node.js 24 both passed; the run completed successfully. No local game rebuild or new visual/performance/listening assessment was needed or run.
- **Deployment:** Vercel reports [success for `180ebcc`](https://vercel.com/rayidalis-projects/borough-drive/CmJTcu9UHznb9nJzGvHs9BXeHzVb). Live HTTPS checks confirm `/` exactly matches `dist/seventh/index.html` and `/index.html` exactly matches `dist/legacy.html`. Root SHA256 `3b6cdbc901933ecf5607c82a44f6426fa2e81c6e84fc481ca51a2d5809aaccca`; legacy SHA256 `ad8fa5a259fea000caa0a463ce636ce5004dad5641d4c9f324930acdaacbb908`. Python's local certificate setup failed initially; system curl completed both checks with TLS verification enabled. No DNS/custom-domain change or check.
- **Remaining scope:** the skill is ready to use as Markdown; brand/favicon/music work and the next street are unimplemented. Visual endorsement, zero calibrated multi-view acceptance and unmeasured cheaper-model cost/quality retain the preceding checkpoint's distinctions.

**Next concrete step:** follow the user's next bounded task using the skill or experience brief. Do not start implementation, reference acquisition or background jobs from this publication request.

## Previous checkpoint — September 21, 2026: portable reconstruction skill and experience brief

Recorded **2026-09-21T16:26:15-04:00** (America/New_York), documentation-only session. The user says the current Seventh Street quality is a good model for the wider project, wants reusable instructions so lower-effort models can reproduce that attention to detail, and explicitly asks **not to implement anything yet**. They supplied `drivearoundnyclogo.png` and want future branding/favicon/site polish plus louder, better, calming lo-fi music. No next street is assigned.

- **Created:** repository-local [reconstruct-street skill](model-source/skills/reconstruct-street/SKILL.md), detailed visual checklist with actual Seventh examples, source/build adapter and copyable worker/reviewer packets. Any assistant can read these Markdown files; no global installation, paid runner or provider-specific integration was created. [Experience brief](docs/DRIVEAROUND-EXPERIENCE-BRIEF.md) records the inspected logo and existing UI/audio, desired behavior and future review criteria.
- **Quality decision:** current revision 04 is now the user-endorsed **visual direction/reference** for future work. This does not accept every facade/minute detail or turn it into a calibrated photographic benchmark: the existing ledger still has 39 source/elevation reviews and zero calibrated multi-view passes. The skill separates useful visual-standard candidates from stricter photographic claims and provides explicit omission/repair gates. Cheaper-model reconstruction quality/cost remains unmeasured.
- **Exact baseline:** branch `main`, session-start HEAD and cached `origin/main` both `e65e8356f2c75f11fba5f511a0c3cea77c026dac`; gameplay/source `d4fbafe9a6af4497de81e2220d31c878f930612e`, routing/verification `bfbc8d764f838708f753a53de9b7f054101b45f0`. Current work is uncommitted Markdown only. No game/model/media/export changes.
- **Source audit correction:** `build.json.sourceCommit` is inherited from `slice.json`'s hardcoded geographic baseline `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`, **not checkout HEAD**. Older checkpoint/report wording below is inaccurate on that field. Record actual gameplay Git commit separately and use exact content/source hashes to identify exports. The new adapter and current engine README explain this; no runtime manifest was edited.
- **Local inputs:** supplied logo remains unchanged and untracked, 2000×2000 opaque PNG, SHA256 `9156dec61cc0ab10adb5b471e7f80d77d4e79102c2a4069e09227f93462f5448`; private September 14 screenshot remains untracked/excluded. No logo derivatives, favicon, sound or geometry were generated. Preserve both original inputs.
- **Validation complete:** bundled skill validator passed; 347 relative paths across 15 changed/new Markdown files resolve; whitespace and documentation-only diff checks pass. Logo/private screenshot hashes match their baselines. Independent source and brand/audio audits plus inspection of selected saved game captures informed the guide. A fresh-context hypothetical East 8th planning exercise preserved scope and unknown evidence, and prompted explicit scope-bootstrap/corner-boundary guidance; it did not reconstruct/evaluate a new street or measure cheaper-model costs. No new game, browser, audio listening, performance or live deployment check was run; historical evidence retains its original limits.
- **Remote/deployment:** no fetch, commit, push, deployment or domain change in this session. Last recorded live verification is September 15 for `bfbc8d7` at `https://borough-drive.vercel.app/`, root=Seventh and `/index.html`=legacy. Live state and custom-domain status not rechecked. Current canonical text is not proof of a connected domain.

**Next concrete step:** resume only from the user's chosen bounded task: implement the brand/audio brief or assign a street and endpoints using the skill. Do not launch either job, repeat the 1:1 backlog or begin an autonomous pipeline merely because these instructions exist.

## Previous checkpoint — September 15, 2026: Seventh build live at Vercel root

Recorded **2026-09-15T17:41:00+00:00**. The user requested that the latest Seventh browser version open at `https://borough-drive.vercel.app/` and asked to fix the failed jobs. Commit `bfbc8d7` is pushed to `origin/main`, Vercel reports a successful production deployment, and the public root now serves the Seventh shell. The legacy Three.js game remains available at `/index.html`.

- **Gameplay/source baseline:** unchanged `d4fbafe9a6af4497de81e2220d31c878f930612e`; prior documentation checkpoint `b36a612`. New routing/documentation commit follows it.
- **Root-routing repair:** the first rewrite deployed but did not replace the physical `dist/index.html`, because Vercel gives filesystem files precedence over rewrites. The original file is now preserved as `dist/legacy.html`; `/` rewrites to `/seventh/index.html`, while `/index.html` rewrites to the legacy page. The local server mirrors these routes. The Godot shell's `/seventh/` base preserves JS/PCK/WASM paths while the browser URL remains `/`.
- **Failed-job repair:** the two failed jobs were GitHub Actions Node checks, not failed Vercel deployments. The shell edit had left `dist/seventh/build.json` with stale `index.html` size/hash data. The manifest and source-shell hash are synchronized, and the original-game verification scripts now inspect `dist/legacy.html`.
- **Validation:** local `npm run verify` and `npm run seventh:verify` pass; local HTTP checks confirmed root=Seventh, `/index.html`=legacy and the expected PCK size. GitHub Actions **Node.js 22 and Node.js 24 both pass** for `bfbc8d7`. Vercel status is successful. Live HTTP checks confirm `/` contains the Seventh shell and `/seventh/` base, `/index.html` contains the legacy ten-block page, and `/seventh/index.pck` returns 200 with **86,494,928 bytes**. No model, PCK/WASM, controls or fidelity records changed. The private screenshot remains untracked and excluded.
- **Remote/deployment:** `bfbc8d7` is pushed to `origin/main` and deployed at `https://borough-drive.vercel.app/`. The custom `drivearound.nyc` domain remains unconnected.
- **Next concrete step:** resume from the user's next direction. Root routing and checks are complete; do not start additional modeling or fidelity work automatically.

## Previous checkpoint — September 15, 2026: 1:1 acceptance explained; session ready to close

Recorded **2026-09-15T15:21:02+00:00**. After revision 04 was saved, the user asked **what it takes to certify 1:1**, then requested Markdown updates so they can close and continue later. This follow-up is discussion/documentation only. No additional modeling, reference acquisition, export, game tests or publication was started.

- **Exact gameplay/source:** `d4fbafe9a6af4497de81e2220d31c878f930612e` on `feature/seventh-lofi-engine`. Previous checkpoint documentation: `b7d1f27`. The new documentation save follows it; obtain its own hash with `git log --oneline -- CODEX-HANDOFF.md SESSION-LOG.md`. The gameplay, included runtime and saved evidence are unchanged. Full implementation and exact tested-package distinctions remain in the preceding checkpoint below and [revision 04 report](model-source/SEVENTH-ENGINE-04.md).
- **User objective retained:** all 39 mapped Seventh-facing properties between First and Second Avenues, both avenue-addressed corner faces and the vacant church/fire-site street edge; precise doors/windows/access, individual shops/signs/materials and observed street/construction details. Preserve the original ten-block map and accepted corner. DriveAround.nyc branding, bounded live addresses and independent orbit/pan/zoom are implemented locally. Lower-cost agents remain the preference for actual coding, with coordinating reference/integration review.
- **Acceptance clarification:** “certified 1:1” is our project shorthand for documented visual acceptance, not an external certificate. It has not been achieved. Rechecked the coverage ledger: **39/39 source views inspected, 39/39 game elevations inspected, 0/39 multi-view calibrated, 0/39 certified**. Actual detail corrections remain, including opening proportions, profiles/ornament, lettering, material scale and incompletely visible features; this is not merely missing paperwork. [Meaning and proposed next pass](model-source/SEVENTH-FIDELITY-BENCHMARK.md#what-passing-11-means-in-this-project).
- **Proposed next fidelity work, not started:** establish dated target conditions and resolve conflicting/occluded sources; bring the working 50/48½ pair through corrected modeling, annotated photo/game alignment and an independent second-view check; obtain user review of that concrete benchmark, then require the same evidence and pass/fail gates for every remaining property and the street/site details. The pair is a starting comparison, not a reduction of scope. Proposed image-space error thresholds do not establish measured world dimensions or literal surveyed 1:1 accuracy. No reference-date policy, numeric tolerance or new facade has been accepted by the user in this follow-up.
- **Local save and privacy:** this follow-up changes only `AGENTS.md`, this handoff, `SESSION-LOG.md`, `START-HERE.md`, `model-source/NEIGHBORHOOD-NOTES.md` and the fidelity benchmark. Preserve the private untracked September 14 screenshot; it remains excluded. No runtime/export/model job remains. Restart using `npm run dev` and open **http://127.0.0.1:5173/seventh/**; recovery does not depend on a surviving process or chat history.
- **Checks actually performed:** inspected branch/status/recent commits against the previous checkpoint, read current project notes and counted the acceptance ledger. Reviewed documentation changes, relative links and whitespace. No game/performance tests were rerun for this documentation-only follow-up; previous results and their limits remain unchanged.
- **Remote and deployment:** no fetch, push, merge, deployment, DNS or domain connection. Cached `origin/main` remains `8792ce4`; local `main` remains `b4120fa`. Production was not rechecked. The saved candidate remains local and unpublished.

**Next concrete step on resuming:** read this checkpoint and the [acceptance clarification](model-source/SEVENTH-FIDELITY-BENCHMARK.md#what-passing-11-means-in-this-project), compare Git status/history, then continue from the user's next direction. Do not automatically start the proposed fidelity pass, unattended acquisition pipeline, wider streets, paid batches or publication. Browser performance and photographic acceptance remain separate; no new 500-user, universal-device or live-domain claim is established.

## Previous checkpoint — September 15, 2026: First–Second revision 04 saved locally

Recorded **2026-09-15T06:13:00+00:00**. The user narrowed the active fidelity request to **East 7th Street between First and Second Avenues**, with every property/business/access/sign and observed street/construction feature represented. They also requested **DriveAround.nyc branding**, a live address label limited to that block, browser performance and freely moving the camera without moving the car or switching POV. Lower-cost agents handled coding; root handled source inspection, visual review, integration and saving. This is a reviewed local candidate, **not certified 1:1**.

- **Exact gameplay/source save:** `d4fbafe9a6af4497de81e2220d31c878f930612e` — Refine Seventh block and add DriveAround camera and address HUD. Branch `feature/seventh-lofi-engine`, resumed from docs `48620a7` / gameplay `34bf6c40e43834918aed2e8e0f4293f3fada8e0f`. Source, required browser package, evidence and current instructions are committed locally. This handoff/log follow in a separate documentation commit; find their own hash in Git history. No unfinished export, model, review or worker job remains.
- **Play and camera:** `npm run dev` → **http://127.0.0.1:5173/seventh/**. Right-drag orbits/looks; Shift + right-drag or middle-drag pans up toward roofs; wheel zooms; V recenters only the selected camera. C selects the four POVs and R resets the car. Inspection does not move/rotate the car; driving still works while looking around. [Controls/rebuild](engine/first-seventh/README.md), [actual rooftop view](docs/images/seventh-engine-04/camera/roof-inspection.png).
- **Block work:** all 39 scoped frontage records source-reviewed and final elevations inspected. 28 GLBs changed, all between First and Second; all five accepted corner GLBs unchanged. Corrected visible entrances/basement access, shop partitions, materials, tile, signs and street structures; added observed dining enclosures, parked bicycle, source-positioned trees/fixtures and dated fire-site conditions. Original ten-block geometry, legacy source namespaces and existing car/drift/audio remain unchanged; original website changes are branding text/metadata only. [Revision 04 report](model-source/SEVENTH-ENGINE-04.md), [preservation](model-source/engine-review-2026-09-15-04/preservation.json).
- **Evidence:** inspected 41 bounded Street View images from September 2024 / April 2026, independent Kinka exterior and July 2026 LPC existing-condition material; [references](model-source/west-seventh-reference-04.json). Saved **145 actual game images**: 128 final-package source-camera/elevation/street captures, seven contact sheets, nine final address captures and a roof-camera demonstration. Root inspected all 39 final elevations and selected detailed/context views; not every capture is calibrated acceptance. [Visual manifest](model-source/engine-review-2026-09-15-04/visual-captures.json), [39 review states](model-source/west-seventh-coverage-04.json). Removed 54 task-owned temporary source image/PDF files; earlier references remain preserved. The private untracked screenshot is unchanged and excluded, SHA256 `a182ce14205d2c4d6125f5f332b80ff643b77b56f0117678e7a2d83541fd652d`.
- **Rendering/performance:** equivalent static materials share global batches, weather updates avoid redundant writes, and individual building culling remains. A 48 m street-bucket experiment increased draw calls and was rejected. Final isolated M1 / 8 GB, Brave, local HTTP, 1440×1000/DPR 1 samples were **59.98–60.02 FPS**, mean **60.00**, all full scale and zero contacts/errors/warnings/failed requests. Ready time **5.341 s**. Current browser ceiling was about 60 FPS; historical rev03 had 75, so this is not a same-environment speedup claim. [Exact measured package/results](model-source/engine-review-2026-09-15-04/performance-final.json).
- **Behavior:** all four camera inspection modes, car-transform invariance, menu/pause gating, pan release over HUD, V recenter and driving while orbiting passed, including an actual roof inspection. Nine address cases passed on the saved package, including 48½, 73–75's negative ID, both avenues and hiding off-block. Clean native camera/address tests passed. Full browser controls/drift/12 camera-weather combinations, preferences, focus pause, Retina sizing, postcard and injected download/context-loss recovery passed before the final static-batch-only change. Native nine-waypoint circuit **66.653 s**, full **545 m** Seventh traversal **24.880 s**, both zero contacts; building collision/reverse/reset passed. [Camera](model-source/engine-review-2026-09-15-04/camera-browser.json), [addresses](model-source/engine-review-2026-09-15-04/address-browser.json), [native](model-source/engine-review-2026-09-15-04/native-final.json).
- **Exact-package distinction:** after the final performance/native/camera tests, only Big Bar's sign moved 0.21 m forward to clear its awning. Runtime scripts, triangle counts, materials, collision envelopes and batching did not change. Those reports keep their actual tested hashes and explicitly qualify this final cosmetic repair. Saved-package visual and address checks followed it. No later untested behavior change is hidden in the performance claim.
- **Package/checks:** 182 models, **3,362,194 source triangles**; PCK **82.49 MiB**, full runtime **120.57 MiB** raw. Saved PCK SHA256 `15b96df07f67a66862794ed1bff06f8f0a48a3d95644a82b368577134b8a81fe`. `build.json` records the pre-export checkout HEAD plus exact source/content hashes; the source commit above saves those bytes. Original `npm run verify`, final `npm run seventh:verify`, all 182 GLB hashes, five corner hashes, original namespaces/controller/audio, staged whitespace, 503 relative Markdown links and 201 candidate-file credential scans passed with zero secret matches. These checks do not establish visual precision or capacity.
- **Remote/deployment:** no fetch, push, merge, DNS, domain connection or deployment. Cached `origin/main` remains `8792ce4`, local `main` remains `b4120fa`; production was not rechecked. DriveAround.nyc is implemented as branding/domain intent, not a verified live connection. No GPU rental, autonomous acquisition pipeline or paid model batch was started.

**Next concrete step:** review the local block and independent camera controls with the user, then resolve the remaining bounded photographic calibration/detail gaps against [the benchmark](model-source/SEVENTH-FIDELITY-BENCHMARK.md). No frontage is newly user-accepted or certified 1:1. Dates, occluded details, exact lettering/profiles and dimensions remain explicit limits; 118 Second's complete long face is not covered by its close source view. Wider streets, automatic cheaper-model production, publication and domain connection are separate future direction. Do not automatically start another job from this backlog.

## Previous checkpoint — September 14, 2026: Seventh architectural fidelity revision 03 saved locally

Recorded **2026-09-14T23:26:02-04:00**. The user supplied a local comparison screenshot and requested implementation across **all Seventh Street**: recognizable individual houses, precise doors/windows/labels/access and a concrete benchmark for later cheaper-model work. Revision 02's resemblance is insufficient. This authorizes bounded Seventh modeling and reference discovery; wider streets, an autonomous production pipeline and publishing remain deferred.

- **Source/save:** branch `feature/seventh-lofi-engine`, resumed at documentation `afcb746` / gameplay `2a43c9fc2519c81b32fc5b5587382e983dd378c6`. **Exact revision 03 gameplay/source commit: `34bf6c40e43834918aed2e8e0f4293f3fada8e0f`** — Refine Seventh Street architecture and define fidelity benchmark. Source, required runtime, benchmark and 137 review images are saved locally. This handoff/log follow in a separate documentation commit; use Git history for their own hash. No further game/source changes or unfinished export/test job remain. Preserve the private untracked screenshot; SHA256 `a182ce14205d2c4d6125f5f332b80ff643b77b56f0117678e7a2d83541fd652d`. No source-photo or unrelated desktop pixels are included in the game or review assets.
- **Play:** `npm run dev` → **http://127.0.0.1:5173/seventh/**. The included player needs no engine/API key/backend/GPU server to run. Existing drift, sound and four cameras are unchanged. [Controls/rebuild](engine/first-seventh/README.md).
- **Implemented:** 76 explicit architectural schedules and six preserved corner/landmark recipes cover the entire 82-frontage inventory; 1,547 window groups, 77 entrances and 17 explicitly provisional unlettered ground units. Added Seventh-only windows/doors/access/masonry/ornament components, posed browser review, structural schedule checks and a [concrete fidelity benchmark](model-source/SEVENTH-FIDELITY-BENCHMARK.md). Original ten-block runtime/namespaces and five accepted engine corner GLBs remain unchanged. [Revision 03 report](model-source/SEVENTH-ENGINE-03.md).
- **Comparison case:** Street View's camera label 49 E 7th points across to **50 and 48½**. Corrected 50's two current at-grade oak entrances, columns/arch, grouped windows, inscription/address and notice case. Adjacent April 2026 viewpoint `py0O1VK0amcbISU49EuZXQ` resolves 48½'s single door/fixed left sidelight, literal **48 1/2** label, white cornice/brackets, projecting shop bays/recessed entrance, open metal benches and pale solid stone hoods. Exact contours, dimensions, letterforms, interiors and hidden access remain estimates/unresolved.
- **Visual evidence:** all 67 archive images and bounded dated street views inspected, with exclusions recorded. All 82 native elevations inspected; final 48½ correction re-inspected, other 181 GLBs byte-identical to the earlier full-street review. Saved **137 actual game images**: 14 final contact sheets, 117 final-package perspective/reference/entrance views, six driving/shop captures. Capturing all views is not full photographic acceptance. [Images/hash manifest](model-source/engine-review-2026-09-14-03/visual-captures.json), [82-frontage status](model-source/seventh-street-coverage-03.json). Thirty-six temporary Google inspection files from this task removed; durable metadata/observations retained.
- **Actual final browser:** Apple M1/8 GB, Chromium/Brave, local HTTP, 1440×1000/DPR 1/full scale. Nine short driving samples **62.84–75.00 FPS**, mean **70.69 FPS** (4.6% below revision 02); local startup **5.920 s** with HTTP cache disabled and compilation caches warmed. Dusk/rain vary and individual samples regress more than the mean; sustained 60 FPS/public-network/device/500-user capacity is not established. [Exact tested package/results](model-source/engine-review-2026-09-14-03/browser-final.json).
- **Behavior/checks:** all 12 camera/weather combinations, steering/brake/reverse/pause/reset, real-key drift **29.22° → −2.70°** after countersteer (zero contacts), saved settings, actual focus pause, Retina pixel cap, postcard and both injected download/context-loss retries passed. Zero healthy errors/warnings/failed requests. Final native nine-waypoint circuit **66.924 s**, full **545 m** Seventh traversal **24.874 s**, zero contacts; collision/reverse/reset passed. `npm run verify` and `npm run seventh:verify` passed; all 182 GLB hashes, five corner hashes, original tree/legacy namespace and vehicle/audio preservation checked. Staged whitespace, 472 relative Markdown links and 177 candidate files checked for local credential values passed, with zero matches. These are runtime/structural checks, not 1:1 certification.
- **Package:** 3,346,371 source triangles; PCK **82.10 MiB**, full runtime **120.18 MiB** uncompressed. Final PCK SHA256 `e6279599b212cbc07753871e25af98e8246513a9059845abb677995c4aa05654`. Final browser and perspective captures use these exact bytes; no later game change. Blender/Godot caches remain regenerable/ignored; keep `dist/seventh/` tracked. Native captures must follow rebuild/import to avoid cached old GLBs.
- **Remote/deployment:** no fetch, push, merge or deployment; production not rechecked. Local-only candidate. No automatic or paid model job/pipeline started.

**Next concrete step:** review 50/48½ against the saved current-view metadata and candidate images, calibrate an independent second view, then resolve each Seventh record's known/uncertain doors, access, profiles and lettering under the benchmark. No frontage is newly user-accepted or certified 1:1; the benchmark target is defined, its initial exemplar still needs acceptance before cheaper-model production. Other streets and publication are separate future scope.

## Previous checkpoint — September 14, 2026: drifting and Seventh Street revision 02 saved locally

Recorded **2026-09-14T20:06:44-04:00**. The user accepted the Godot slice's visual direction, then authorized better controls, drifting and engine sound, and highly detailed **Seventh Street**, with discovered references, stronger surfaces/trees/lighting/street furniture and smooth browser play. The stated full-street assumption after the unanswered optional extent question was Second Avenue through Avenue A in the original map, retaining boundary context. The eventual goal is every part of the original map; this session covers Seventh only. The new pass is ready for review, with the every-inch fidelity target still open.

- **Exact new gameplay/source commit:** `2a43c9fc2519c81b32fc5b5587382e983dd378c6` — Improve drifting and sound, detail the full Seventh Street slice. Saved locally on `feature/seventh-lofi-engine`, resumed clean from documentation `41eafac` and previous engine `1b07429987e6478b51e8579e6bbc9c8664e68047`. Only this handoff/log remain for the following documentation commit; identify its own hash with Git history. No unfinished model/export job.
- **Play:** `npm run dev`, then **http://127.0.0.1:5173/seventh/**. Included runtime is in `dist/seventh/`; no engine install, backend, API key or GPU server is needed to play. W/S accelerate/brake/reverse, A/D steer, **turn + Space then release/countersteer to drift**, C cameras, T weather, M sound, R reset. Desktop keyboard play is tested. [Controls/rebuild](engine/first-seventh/README.md).
- **Implemented:** planar momentum and tire grip, recoverable dry/wet drifting, automatic gears/RPM, six original car-audio layers, capped fading tire marks. Entire mapped Seventh Street is connected; **182 building objects, all 82 Seventh frontages**, original return circuit retained. Shared mipmapped masonry/normal maps, 15 new individual shop designs, distinctive 50 East 7th profile, branching leaf trees, evening lamps, hydrants/mailboxes/drains and concrete sidewalks. [Revision 02 report](model-source/SEVENTH-ENGINE-02.md).
- **Reference/visual evidence:** 67 archive photos and 39 additional images inspected, with wrong-address/interior/illustration/temporary-object exclusions. [Source record](model-source/seventh-street-reference-02.json), engine-only `storefront-details.json.seventhEngine` and `seventh_street_kit.py`. All 82 native elevations inspected; 14 contact sheets and 42 actual browser captures, including all 15 new shop closeups, are saved in [evidence images](docs/images/seventh-engine-02/) with [hash manifest](model-source/engine-review-2026-09-14-02/visual-captures.json). Orthographic elevation captures are not matched photographic cameras. Close review fixed dropped material tags, storefront backing/stone-band occlusion, awning winding and Ladybird's door proportions/relief.
- **Final browser performance:** [exact tested build and results](model-source/engine-review-2026-09-14-02/browser-final.json), Apple M1/8 GB, Brave/Chromium, local HTTP, 1440×1000/DPR 1. Northbound golden/dusk/rain **74.8/75.0/75.0 FPS**; six east/west Seventh samples **72.0–75.1 FPS**, all at full render scale with zero contacts. Final local startup **4.95 s**, HTTP cache disabled but browser/driver compilation caches warmed. Hidden street uploads before play avoid the earlier first-turn hitch. No universal frame-rate or public-network promise.
- **Final browser behavior:** zero healthy-runtime errors/warnings/failed requests. Real-key drift **18.10°** to **−2.59°** after countersteer, zero contact. Brake/reverse/steering/pause/reset, all 12 weather/camera combinations, preferences across reload, actual tab-switch pause, Retina 1100×800/DPR 2 pixel cap and actual postcard PNG passed. Four injected HTTP 503 pack failures and forced WebGL context loss each showed visible recovery and successful Retry. Expected fault errors are separate. The test runner's reload DOM race was fixed.
- **Other checks:** deterministic handling passed dry/wet slide, catch, symmetry, brake/reverse, gear/RPM, reset and 30/60/120 Hz integration. Original synthesized source WAVs are nonempty/unclipped; listening quality remains for user review. Native nine-waypoint circuit **66.609 s**, full **545 m** Seventh traversal **24.904 s**, both zero contacts; frontage collision/reverse/reset passed. Native driving preceded final surface/door/awning presentation fixes; controller and collision layout stayed unchanged. `npm run verify` and `npm run seventh:verify` passed. Final binary check validated all 182 GLB hashes and 358 RGBA material primitives. Staged whitespace and 430 relative Markdown links passed before this checkpoint. CI now includes Seventh verification; no remote CI run occurred.
- **Reproduction/package:** **3,431,884 source triangles**. PCK **83.21 MiB**, full runtime **121.29 MiB uncompressed**; estimated Brotli PCK+WASM **82.52 MiB**. Eliminating redundant UV/tangent arrays reduced the detailed package's PCK about 27%; it remains larger than the old 154-building slice. [Package and 68 source hashes](dist/seventh/build.json). Final PCK SHA256 `66cd84e12565f2a4971187c2d02e1080538aa94e3900bf56bfdfddb4c0b40a0f`. Godot 4.7.2 and Blender 5.2.1 used; ignored tools/import/GLB caches are regenerable. Keep included `dist/seventh/` tracked.
- **Preservation:** original ten-block gameplay `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`; original `dist/index.html`, prototype, reconstruction/vendor assets, source-data, core/corner recipes and old storefront namespaces remain unchanged. [Preservation check](model-source/engine-review-2026-09-14-02/preservation.json). New generic maps are existing Poly Haven CC0 materials; no new reference-photo pixels are distributed. Credits/licenses retained.
- **Push/deployment:** no fetch, push, merge, remote branch creation, rental or deployment. Cached `origin/main` remains `8792ce4`; local `main` remains `b4120fa`. Production was not checked or changed this session; September 10 evidence below is historical. New source and documentation are local only.
- **Remaining fidelity/capacity:** the [82-frontage coverage record](model-source/seventh-street-coverage-02.json) explicitly retains inherited/estimated businesses and incomplete present-day exterior evidence. Exact lettering/artwork, unmeasured proportions/ornament, interiors, occluded features and movable-object positions remain unresolved. This is not every facade completed to the accepted corner standard or a 1:1 acceptance claim. First download remains heavy; no public-network/mobile/browser matrix, traffic, shared-world multiplayer or 500-user load test. The autonomous imagery pipeline is still unimplemented.

**Next concrete step:** review the local driving feel/sound and street pass with the user. Continue Seventh's unresolved facade details from their feedback and the saved coverage/evidence before treating its fidelity target as complete. Preserve the accepted core/corner and original game. Other streets, publication and autonomous/paid processing require later direction. Read this checkpoint and latest log, then inspect Git before further edits.

## Previous checkpoint — September 14, 2026: playable Godot browser slice saved

Recorded **2026-09-14T17:55:24-04:00**. The user asked for a faster, aesthetic lofi browser driving game around First & Seventh, with weather, proper streets and multiple driving viewpoints, eventually accommodating approximately 500 simultaneous independent players. After discussing Unreal streaming, they said a GPU machine is unavailable and instructed us to proceed with available tools. The completed local prototype uses **Godot 4.7.2 Compatibility / WebGL 2**, running on each player's device. This supersedes the earlier Godot pause and GPU dependency.

- **Exact new gameplay/source commit:** `1b07429987e6478b51e8579e6bbc9c8664e68047` — Build First and Seventh lofi browser driving slice in Godot. Saved locally on `feature/seventh-lofi-engine`, based on investigation commit `6d21a27` and original main `b4120fa`. Only this handoff and session log remained for the following documentation commit; identify its own hash in Git history.
- **Original game:** `c76dccebc143960498d21eeab8c55c2d0dbcc5ce` remains the existing ten-block gameplay/source. Original `dist/index.html`, prototype, reconstruction/vendor assets, source-data, corner observations and protected core remain unchanged. New required player assets are isolated in `dist/seventh/`; original source credits and photographic limitations are preserved.
- **Play now:** `npm run dev`, then **http://127.0.0.1:5173/seventh/**. The route returned HTTP 200 and was opened in Brave for user review. The original game remains at `/`. The exported browser package is included; playing needs no Godot/Blender install, API key, backend or GPU server. Restart the server normally after closure; no old process/session ID is required.
- **Implemented:** authored apricot/cream coupe, wheel/body animation, steering/braking/reverse, frontage/boundary collision; chase/hood/cockpit/overhead cameras with smooth following and obstruction checks; golden hour/dusk/rain; existing First–Second–Seventh–St Marks circuit; map/HUD, focus/pause/reset, original synthesized audio, saved camera/weather/sound preferences, downloadable postcards and visible loading/graphics Retry. [Project and controls](engine/first-seventh/README.md), [review report](model-source/SEVENTH-ENGINE-01.md), [actual screenshots](docs/images/seventh-engine-01/).
- **Browser evidence:** [final review](model-source/engine-review-2026-09-14/browser-final.json), M1/8 GB/Brave Chromium 153, 1440×1000 CSS pixels/DPR 1, local HTTP. Short northbound samples: **74.8/75.0/75.0 rendered FPS** in golden/dusk/rain at full 3D scale, about 485–486 calls and 2.86 million submitted triangles. Final local startup 3.62 s with browser/driver compilation caches warmed by earlier tests; first playable startup 9.26 s. Healthy gameplay recorded zero errors/warnings/failed requests. These are smaller-scene measurements, not equal-scene engine comparisons, universal frame-rate promises or a 500-user test.
- **Checks completed:** actual browser inputs and all 12 camera/weather captures; reverse, brake, steering, reset/pause, preferences across reload, Retina 1100×800 at DPR 2 remaining 1100×800 rendered pixels, real tab-switch pause and actual PNG postcard download. Four injected HTTP 503 responses produced a visible download failure; Retry restored play. Forced WebGL context loss produced the in-page recovery screen and successful Retry. Expected injected errors are isolated in the report. The engine's blocking context-loss alert is suppressed by the shell's earlier capture listener.
- **Native circuit:** [review](model-source/engine-review-2026-09-14/native-final.json) drove all nine route waypoints in 65.986 s with zero wall contacts; separate frontage collision, reverse and reset checks passed. Final cockpit/road presentation was subsequently checked in the browser; vehicle/world physics remained unchanged.
- **Build/preservation checks:** `npm run seventh:verify` validates 11 package files, 25 source hashes, all 154 building identities/five corner objects and original geographic hash. `npm run verify` passed for the existing game, including 2,308 road samples and existing batching/presentation checks. Original runtime/source preservation, staged whitespace and 397 relative Markdown links checked. After runtime review, only generated HTML/notice trailing whitespace was normalized; engine, game, JS and worklet hashes remain identical to those tested. [Package/source hashes](dist/seventh/build.json).
- **Reproduction:** source in `engine/first-seventh/`, exporter `model-source/export_seventh_engine.py`, audio recipe `model-source/make_seventh_audio.py`. Official Godot 4.7.2/templates are cached in ignored `.tools/godot/`; Blender 5.2.1 was used. The approximately 201 MiB GLB cache and Godot import/staging folders are ignored and regenerable. `npm run seventh:build` validates source GLBs, imports, stages the export and includes notices/hash metadata. Keep `dist/seventh/` tracked as required runtime assets.
- **Push/deployment:** no fetch, push, merge, remote branch creation, GPU rental or deployment occurred. Cached `origin/main` is still `8792ce4`; local `main` remains `b4120fa`. Current external state was not rechecked. September 10's verified production remains the historical evidence below; the new slice is local only.
- **Remaining limits:** initial package is about 102 MiB before HTTP compression (about 64 MiB PCK plus 38 MiB WASM), so first-load weight remains material. No public-network startup, broader device/browser matrix, mobile touch controls, shared-world multiplayer, traffic or 500-user load test. Geometry/extra props/materials retain stated uncertainty; the user has not accepted this new art direction. Vercel can distribute the static build, but launch bandwidth/plan capacity still needs assessment. Wider map fidelity and the autonomous reference pipeline remain deferred.

**Next concrete step:** review the local slice's graphics and handling with the user, then address their bounded feedback and startup/device performance. Preserve the original game and accepted corner/core. Do not treat this completed prototype as permission to expand, start imagery/paid jobs or publish. Read this checkpoint, AGENTS, START-HERE, the neighborhood notes and engine review; inspect Git before editing.

## Previous implementation milestone — September 14: proceed using available hardware

The user cannot obtain a GPU machine now and explicitly instructed: **“go ahead and make whatever you can with what you have access to.”** Build the bounded First & 7th playable slice locally, using the available Godot browser export. This supersedes the compute dependency and Godot pause below. Preserve the lofi aesthetic, weather, multiple driving cameras and eventual browser-scale ambition. No paid compute, wider imagery production or publication is authorized. Current branch `feature/seventh-lofi-engine`, resumed clean at `6d21a27`; original game/source remains `c76dcce`. Implementation and actual engine/browser validation are underway; do not describe the earlier scaffolding as a finished game.

**Mid-session milestone:** The Godot game and separate `dist/seventh/` browser export now run. Implemented authored coupe and collisions, four driving cameras, golden/dusk/rain transitions, street circuit, interface, original synthesized audio, postcard capture, saved preferences and adaptive resolution. [First passing browser review](model-source/engine-review-2026-09-14/browser-first-playable.json) records 66.4/72.1/72.1 rendered FPS in short golden/dusk/rain driving samples at 1440×1000/DPR 1, approximately 504–510 draw calls and 3.0 million submitted triangles; startup 9.26 s on unthrottled local HTTP, zero recorded errors/warnings/failed requests. Controls, weather/cameras, reverse/brake/reset/pause and settings reload passed. This is the smaller Godot slice, not an equal-scene benchmark or a 500-user test. Original existing dist/scripts/source-data files remain unchanged at this milestone. Initial export is about 64 MiB PCK plus 38 MiB WASM before HTTP compression; startup payload needs explicit reporting. Browser diagnostics fixed a JavaScriptBridge boolean/int comparison that halted startup. Native tests and screenshots are being rerun after visual changes; new route-map, softer road/lighting and Retina pixel cap are source changes pending final export/review. Work is uncommitted after `6d21a27`; nothing pushed/deployed. Finish these checks and save a final checkpoint before handoff.

## Previous checkpoint — September 14, 2026 (Unreal browser target; GPU route pending)

Recorded **2026-09-14T15:59:17-04:00**. The user authorized a bounded First & 7th game slice: aesthetic lofi animated graphics, weather changes, proper streets and multiple driving views, with GTA Vice City as a gameplay reference. They then clarified **Unreal in the browser**, eventually serving approximately **500 simultaneous independent players**. Godot implementation was paused at that clarification. Virality is an aspiration, not a guaranteed result. The September 12 implementation pause is superseded for this slice; automated imagery jobs and wider reconstruction remain outside the task.

- **Current branch:** `feature/seventh-lofi-engine`, created from clean `main` at `b4120fa`. Session-start main was one documentation commit ahead of cached `origin/main` (`8792ce4`). Work is saved locally on this feature branch; identify the checkpoint commit in Git history. No fetch, push or deployment this session.
- **Exact existing gameplay/source:** `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`. All existing `dist/`, `scripts/` and `source-data/` files are byte-identical to session-start `b4120fa`. Original models, facade observations and source provenance are preserved. The current public site has not been rechecked; September 10 verified deployment remains the dated evidence below.
- **Delivery plan:** [UNREAL-BROWSER-PILOT.md](model-source/UNREAL-BROWSER-PILOT.md) explains the one-session pilot, art/gameplay scope, Vercel website plus GPU Pixel Streaming architecture, dated cost reference, tests and growth toward 500 independent sessions. A larger ordinary web server cannot fix local rendering; Unreal streaming requires GPU rendering/encoding capacity. Multiplayer in one shared city has not been implemented or assumed.
- **Current dependency:** this MacBook Air has Apple M1/8 GB RAM and approximately 28 GiB free disk at session start; Unreal is not installed. A suitable Windows/Linux GPU development/streaming machine is needed for an actual Unreal build and review. The user was asked whether one is available or whether to prepare a small paid cloud pilot. No answer, provider/account selection, spending approval or remote access has been supplied yet. No purchase, VM, subscription or public upload has occurred. Do not infer approval from silence.
- **Fresh browser evidence:** [baseline](model-source/engine-review-2026-09-14/browser-baseline.json), local Brave/Chromium/M1, 1440×1000/DPR 1, cache disabled. Detailed driving 22.29 FPS; warm Automatic driving 42.18 FPS (last 120 rendered frames 52.96 FPS), reaching ratio .70/AO off. About 6.06 million submitted triangles and 1,187 draw calls per Automatic driving frame across all passes. Baseline completed with zero recorded errors/warnings/failed requests. These short samples are not Unreal results or a concurrent-hosting test.
- **Asset preparation:** [export_seventh_engine.py](model-source/export_seventh_engine.py) exported 154 existing objects around the First/Second/Seventh/St Marks circuit, including all five corner buildings; 2,835,181 triangles. Separate lofi vertex-color material experiments retain geometry. Static GLB/hash/count/source checks passed: [record](model-source/engine-review-2026-09-14/asset-preparation.json). No Unreal import, new material visual acceptance, camera, driving or weather runtime has been completed. Generated approximately 201 MiB of uncompressed GLBs are an ignored local cache; the exporter and tracked slice manifest reproduce them.
- **Paused experiment:** [engine/first-seventh/README.md](engine/first-seventh/README.md) identifies incomplete Godot shaders/mesh helpers and the manifest. It is not a playable game and must not be presented as the requested Unreal result. Official Godot 4.7.2 and its templates were downloaded into ignored `.tools/godot/`; they are optional and not needed for the Unreal continuation. First sandboxed Blender start crashed at graphics initialization; the authorized retry succeeded. No paid service ran.
- **Checks:** fresh existing-browser profile, static experimental export checks, `npm run verify` for the unchanged browser game, source preservation and `git diff --check`. No Unreal compilation, Pixel Streaming test, load test or new aesthetic screenshot is claimed.

**Next concrete step:** resolve the GPU machine/cloud pilot with the user, prepare its exact quote and spending/time limit for approval, then implement, cook, stream and visually review **one** First & 7th Unreal session. Begin with the existing source geometry and preserve the accepted corner/core. Measure sessions per GPU and end-to-end latency before selecting 500-player infrastructure. Do not automatically resume Godot, buy a server, expand the map or publish.

## Previous checkpoint — 2026-09-12-01 (session closed with all Markdown synchronized)

Recorded **2026-09-12T19:40:19-04:00**. The user asked to update all Markdown before closing this session. **The latest implementation direction is still “brainstorming only; don't implement anything yet.”** Next session should continue designing automatic reference discovery and block-by-block reconstruction, without starting modeling, imagery requests, a paid pilot, a scheduler or Unreal. This checkpoint is a documentation-only save.

- **Resumed Git state:** clean `main` at `8792ce4a941165343c5115792b66ce09688c8172`, matching cached `origin/main`. That publication/evidence checkpoint was successfully pushed in the preceding turn. No fetch, push or deployment was performed during this September 12 documentation refresh; current external state was not rechecked.
- **Exact gameplay/source:** `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`. Pass 08 (`219d4e9`) supplies the current corner models/observations; pass 09 improves browser presentation and runtime. All non-Markdown code, models, textures, source data and workflow JSON remain unchanged in this refresh. The only change under `dist/` is the asset-credit Markdown.
- **Saved continuation:** all **23 tracked project Markdown files** refreshed, including hidden PR instructions, source/reference reports, asset/license notices and the paused example worker task. Earlier research, dated measurements, source provenance and session entries are retained. README/start/agent instructions now agree on the latest scope; [BLOCK-WORKFLOW.md](model-source/BLOCK-WORKFLOW.md) explicitly separates the desired automatic process from the existing offline tools. These edits are saved in a local documentation commit; identify its own SHA with Git history. They are not pushed or deployed by this handoff request.
- **Publication actually verified:** September 10 Vercel deployment `3nNFK32d2P9W4E42BnT2aR9ey43G` for `5905b95ff3fcd2c111011ec73097af51dd25e100`, at https://borough-drive.vercel.app. [Publication evidence](model-source/publication-review-2026-09-10.json) records 13 changed files plus 5 preserved assets matching SHA-256, successful GitHub Node.js 22/24 checks in run `34516930981`, and a successful production browser smoke. Later pushed checkpoint `8792ce4a941165343c5115792b66ce09688c8172` had the same `dist/` tree. Its own automatic deployment was not separately verified. These are dated observations, not a September 12 live check.
- **Quality and performance:** First & 7th revision 06 remains the user-accepted minimum; pass 08 plus browser pass 09 is the current working reference. Preserve the First & 10th core. [Pass 09](model-source/BROWSER-PASS-09.md) records M1/1440×1000 Automatic corner 60 FPS and additional warm driving 48 FPS overall / 55.9 FPS over the final 120 rendered frames; idle redraws zero. Driving is not consistently 60 FPS across the map, and this is not a perfect measured 1:1 or AAA result. No new performance, photographic or visual comparison was run for this docs-only refresh.
- **What exists:** offline inventory/packet/validation scripts, 1,348 ownership items (672 facade sections, 615 secondary-surface reviews, 43 street segments, 18 intersections), three-facade pilot plan, empty approved-image registry and an unresolved Tile Bar example. These prepare finite work; they do not execute an autonomous production process. No cheaper-model evaluation or paid API job has run; no scheduled job continues after closing the chat. The saved pilot remains valid with `accepted: false`.
- **What the user wants to discuss next:** a coordinator that chooses a block, automatically finds useful dated full/oblique/detail views, creates small evidence-linked facade/shop/street tasks, uses cheaper workers and reusable modeling recipes, renders comparisons, repairs failures within limits, and presents the block for review. It should persist status, source/version hashes, budgets and retry counts so interruption does not restart completed work. The user should not have to manually feed each image. Automatic acquisition, the persistent execution ledger, paid dispatcher and coordinator/build/review loop are not built. Proposed sequencing is a few facades, one complete block, the remaining existing East Village map, then Manhattan/NYC; this sequence is not approval to begin.
- **Open source/cost issues:** September 10 source review identified Google restrictions affecting the proposed extraction/derived-content workflow. Viewing rather than storing, or manually supplying Google screenshots, does not itself establish suitable permission. Google ingestion remains disabled in the offline policy; suitable permission or appropriately licensed alternative imagery is needed before implementing acquisition. The ignored root `.env` is outside `dist/`; no key belongs in source packets or documents. Google access, model availability, pricing and terms were not rechecked today. The proposed mini model and $0.162 three-job Batch arithmetic are unevaluated, dated planning assumptions, not an end-to-end price or spending guarantee.
- **Other unresolved work:** unclear/hidden details and unmeasured dimensions; whole-map fidelity gaps; sustained dense-view browser performance; semantic review of actual patch records; older compiler resets to three non-corner exported heights (`241822303`, `241829661`, `248142640`) before unattended builds. All remain explicit in the current reports. A gap record, successful export or proposal validation is not completion.
- **Checks for this refresh:** Markdown coverage and relative links, `git diff --check`, unchanged non-Markdown tracked files, and offline validation of the saved example packet. No npm/game/Blender/browser test was rerun because no executable/model content changed. This refresh preserves prior technical evidence rather than claiming new results.

**Resume:** open this repository, read AGENTS.md, this checkpoint, the latest session entry, START-HERE.md, the neighborhood notes and BLOCK-WORKFLOW.md; inspect `git status --short --branch` and recent commits. Preserve the local documentation commit and recheck publishing state before any future push. To view the game, run `npm run dev` and open http://127.0.0.1:5173, selecting **Graphics → Automatic** if Detailed was previously saved. No old server, browser session, temporary photo folder or tool session ID is required. Continue brainstorming until the user gives a new implementation instruction.

## Previous checkpoint — 2026-09-10-09 (published and live-verified; batch process is brainstorming only)

Recorded **2026-09-10T15:03:43-04:00**. The user explicitly authorized pushing the saved work to `main`, and asked to brainstorm an unattended process that finds reference views itself, improves every part of the current map to the First & 7th standard, then eventually covers Manhattan/NYC. **“Don't implement anything yet” applies to new gameplay and job work.** This turn published the existing passes, verified production and refreshed publication/continuation records only. No AI worker, imagery acquisition or batch implementation ran.

- **Exact gameplay/source:** `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`. **Published commit:** `5905b95ff3fcd2c111011ec73097af51dd25e100` (includes the source plus its checkpoint), fast-forward pushed from `477fcdb524407e189f67eb59771d8da84c4a16fe` to the existing `origin/main`. Resumed clean main five commits ahead, fetched first and found no divergence. No force push, new repository or new hosting project.
- **Live deployment:** Vercel success, deployment `3nNFK32d2P9W4E42BnT2aR9ey43G`, https://borough-drive.vercel.app. All **13 changed live files plus 5 preserved assets** matched source SHA-256 over HTTPS, including the three pass 08 corner GLBs, current runtime, core, street-material asset and decoder wrapper. [Publication evidence](model-source/publication-review-2026-09-10.json).
- **Actual validation this turn:** GitHub run `34516930981` passed both Node.js 22 and 24 `npm run verify` jobs. [Production browser smoke](model-source/browser-review-09/live-smoke.json) passed revision 09, fresh Automatic default, five starting sections, mode switch/reset and zero unexpected errors/failed requests. [Live game capture](docs/images/browser-09/live-first-seventh.png) inspected. Initial direct Node asset download and one GitHub detail request timed out; curl and the GitHub retry succeeded. No new local model rebuild, full 26-view review, source compilation, performance benchmark or photographic comparison was needed; prior pass 09 evidence below remains dated history.
- **Local/documentation state:** No game or worker code changed. Publication JSON, live screenshot/smoke, README/start/pass publication wording and these checkpoints are being saved in a separate documentation/evidence commit. Its own SHA/push state is visible in Git history/status; do not amend this checkpoint to insert its hash. Its `dist/` tree is identical to the verified publication. Any automatic deployment of that following documentation commit is distinct from the verified `5905b95ff3fcd2c111011ec73097af51dd25e100` deployment recorded here.
- **Brainstorming proposal, not implemented/approved:** a persistent coordinator chooses a block, assigns each facade/shop/street/intersection/secondary surface, finds dated full/oblique/detail views automatically, records observations, sends small bounded tasks to cheaper models, builds reusable recipes, renders comparisons, repairs limited failures and reports a block for review. Save progress/budget/source hashes so interruption does not restart completed work. Stronger models handle unusual architecture and failed reviews; visual acceptance and unresolved coverage remain explicit. Start by qualifying a few facades, then one complete block before wider map production. User should not have to manually feed each image. Runtime remains browser-first; city scale would need streamed tiles and distance-appropriate rendering, not loading all geometry at once.
- **Current workflow is still offline:** [BLOCK-WORKFLOW.md](model-source/BLOCK-WORKFLOW.md) inventories 1,348 ownership items and prepares/validates three-facade pilot packets. Automatic reference discovery, a persistent execution ledger, paid request dispatcher, coordinator/build/repair loop and automatic publication are not implemented. No scheduled job runs after this chat. Cheaper-model accuracy and end-to-end cost are unevaluated.
- **Google source issue remains:** technical API access works from the prior corner task, but the standard Maps terms §3.2.3 restrict extraction and creating content based on Maps content. Automatically viewing instead of retaining images, or manually uploading Google screenshots, does not by itself establish permission for the proposed derived-content pipeline. A Google adapter would require suitable permission; owner-supplied or appropriately licensed images can enter the same proposed acquisition stage. No new Google imagery request this turn; the local key remains outside `dist/` and Git.
- **Next concrete step:** continue brainstorming the coordinator, automatic source acquisition/permissions and review granularity with the user. Do not implement the pipeline, expand buildings, run paid jobs or resume Unreal without their next instruction. Review the published corner in **Graphics → Automatic**. Prior limits remain: dense driving is not consistently 60 FPS; hidden/unmeasured facade details are unresolved; compiler height resets must be addressed before unattended exports.

## Previous checkpoint — 2026-09-10-08 (browser pass and offline block workflow saved locally)

Recorded **2026-09-10T05:57:24-04:00**. The user chose smooth browser exploration inspired by slowroads.io, deferred Unreal, and requested improvement of First & 7th followed by a repeatable process suitable for cheaper AI workers. The current corner is the working visual reference; no wider modeling, paid batch or publication began. The prior engine assessment is historical advice, superseded by this direction.

- **Exact gameplay/source:** `main` at `c76dccebc143960498d21eeab8c55c2d0dbcc5ce`. This commit contains [browser pass 09](model-source/BROWSER-PASS-09.md), [the offline block workflow](model-source/BLOCK-WORKFLOW.md), updated current documentation and durable review evidence. Source/model baseline was `219d4e9ccec8e019351d3a27d7dff6dcaf07237d`; session-start documentation HEAD was `4682fef`. Only these two checkpoint documents remained modified before their separate save; identify that documentation commit through Git history. No unfinished runtime/model edits.
- **Runtime:** main-depth ambient shading avoids another city draw; Automatic is the default for new visitors and adapts pixel work during motion, restoring ratio 1/contact shading once stopped. Saved quality preferences are honored. Added authored static clouds and bounded First & 7th asphalt/paint variation, plus fixed-step driving-view interpolation. Physical driving, collisions, 72 checked reconstruction assets/manifests/support modules and the accepted First & 10th geometry are byte-identical to pass 08. No Blender rebuild or new photographic detail claim.
- **Actual performance:** Chrome 152 / Apple M1 / 1440×1000 DPR 1, local HTTP: Saifee Detailed 48.9→52.4 FPS; submitted triangles 3.83M→1.92M, calls 596→301. Automatic warm corner 60.0 FPS; additional warm northbound drive 48.0 FPS overall, final 120 rendered frames 55.9 FPS, reaching ratio 0.70/contact shading off. Detailed drive 27.1→32.9 FPS; fixed-frame paths differ in duration/distance. Stopped Automatic restores quality; idle redraws zero. Local ready 671→525 ms, initial reconstruction transfer about 40.76 MB. These short single-device samples do not establish sustained 60 FPS or slowroads.io parity. Final data: [before/after and preservation](model-source/browser-review-09/).
- **Actual checks:** `npm run verify` passed on final runtime (30 modules, 2,308 road samples, unchanged vehicle/source/export/material/spatial invariants, new frame-budget/interpolation tests). `python3 scripts/verify-block-workflow.py` passed; saved unresolved pilot validates against current sources. Final [26-view browser review](model-source/browser-review-09/review.json) passed fresh Automatic default, controls, resets, quality persistence, idle wake, deferred-core failure/retry and startup fallback/retry with zero unexpected errors/failed requests. Inspected final corner/shop/driving captures; 11 selected unedited captures are in [docs/images/browser-09](docs/images/browser-09/). No new source compilation or Google comparison this turn. Whitespace and 308 relative Markdown links checked before this checkpoint; 336 eligible files scanned with no local credential matches. Ignored root `.env` remains 0600, outside served `dist/`.
- **Workflow:** deterministic inventory of 1,348 ownership items: 672 facade sections, 615 secondary-surface reviews, 43 street segments, 18 intersections. Protected core cannot receive a worker packet; v1 emits only unprotected facade packets. Three-task offline plan, minimal Tile Bar packet, permission/source registry and proposal validator are committed. Patch headers/declarations are checked; actual record changes and visual fidelity still require coordinator review. No automatic merge/build dispatcher, completion ledger or paid runner exists. GPT-5.4 Mini is an unevaluated candidate; illustrative Batch token allowance is $0.162 for three jobs, not a measured end-to-end quote or billing cap. Zero model API requests, model evaluations or background jobs ran.
- **Imagery:** current Google Maps terms §3.2.3 restrict bulk extraction/caching and creating derived content. A working API key or deleting previews does not establish permission for the proposed automation. New Google batch ingestion is disabled; use owner-supplied or appropriately licensed images with permission covering processing/derivatives. No such source is registered yet. Preserve existing pass 08 provenance. No credential belongs in a packet or checkpoint.
- **Remote/publication:** no fetch, push, CI run or deployment this turn. Cached `origin/main` remains `477fcdb524407e189f67eb59771d8da84c4a16fe`; source commit is four commits ahead and the following checkpoint will be a fifth. Last verified public source is `522cd1de77b72186dc98e06a3b9f6ce4db761d8b`, verified September 9 at https://borough-drive.vercel.app; current live state was not rechecked. Passes 08 and 09 remain local.
- **Preview/next action:** opened http://127.0.0.1:5173 and confirmed HTTP 200 / revision 09. Restart with `npm run dev` after closure; select **Graphics → Automatic** if a saved Detailed choice remains. First review the corner presentation/speed on the user's device, then register suitable full-facade/oblique images and qualify the cheaper worker on three facade tasks. Measure correction cost and rendered accuracy before expanding to the adjacent block. No automatic migration, wider expansion, paid work or publishing. Dense-view performance, unmeasured/hidden architectural details and the older compiler's three non-corner render-height resets remain unresolved; see the two current reports.

## Previous checkpoint — 2026-09-10-06 (engine direction assessed; game unchanged)

Recorded **2026-09-10T05:02:39-04:00**. The user asked whether the sky, roads and driving can reach a PS5-game standard in the browser, or whether Unreal should provide the final runtime while the current pipeline supplies models. [Engine assessment and proposed corner test](model-source/ENGINE-DIRECTION.md) recommends Blender/source recipes for authoring, Three.js for browser inspection, and an early First & 7th Unreal test before map-wide production. **This is a recommendation, not a user-approved migration decision.** No browser upgrade or Unreal implementation was made in this assessment.

- Gameplay/source remains local `219d4e9ccec8e019351d3a27d7dff6dcaf07237d`. Session-start HEAD `179c994` was clean on `main`, two commits ahead of cached `origin/main`. The only new changes are the assessment and these checkpoint documents, saved in a following local documentation commit; identify it with Git history.
- Read the pass 08 checkpoint below for completed corner work, performance, sources and remaining visual differences. User acceptance remains pending. No new fetch, push, deployment or live verification; September 9 source `522cd1d` remains the last verified publication.
- Read-only inspection confirmed the sky/light setup, flat runtime roads, planar vehicle physics, tile-wide material merging, required Draco compression, external textures and corner vertex colors. Official Epic documentation was checked for atmosphere/Lumen, Chaos, glTF, modular-mesh limitations, macOS hardware and Pixel Streaming. Import and Unreal performance are untested. No game verification was rerun because no game, source model or export changed.
- Proposed next action: settle the engine direction with the user. If they choose Unreal, start with a separate modular export of First & 7th and one driveable street segment; establish materials/lighting/handling and a hardware budget before expansion. If they prefer a browser upgrade, define a bounded sky/road/driving pass and retain performance checks. Do not infer migration, paid GPU hosting or citywide jobs from this recommendation.

## Previous checkpoint — 2026-09-10-05 (Street View refinement committed locally; visual acceptance pending)

Recorded **2026-09-10T04:36:37-04:00**. The user authorized using the working Street View API to improve all four corners of First Avenue/East 7th toward the attention to detail and visual quality of Assassin’s Creed Unity. The bounded five-building, nine-elevation, eight-shop refinement is implemented, exported, reviewed and committed locally. **This remains a stylized reconstruction with documented differences, not a perfect 1:1 or AAA-quality result.** User acceptance of pass 08 is pending; revision 06 remains the accepted minimum standard. Wider expansion and jobs remain deferred.

| Item | Current state |
| --- | --- |
| Exact local gameplay/source commit | `219d4e9ccec8e019351d3a27d7dff6dcaf07237d` — Refine First and Seventh from dated Street View observations |
| Previous published gameplay | `522cd1de77b72186dc98e06a3b9f6ce4db761d8b`, performance pass 07. Session-start checkpoint HEAD was `477fcdb524407e189f67eb59771d8da84c4a16fe`; its uncommitted local API setup and notes were preserved and completed. |
| Branch and local changes | `main`; implementation, three GLBs, sources, helpers, current docs and 37 game PNGs saved in the source commit. Only this handoff and session log remained modified before their separate checkpoint commit. No pending export or browser capture. Identify the checkpoint documentation commit using Git history. |
| Push state | No fetch or push in this refinement turn. Cached `origin/main` remains `477fcdb524407e189f67eb59771d8da84c4a16fe`; current remote state was not rechecked. The new source and following checkpoint commit are local. |
| Deployment state | This pass is not deployed. Production was not rechecked; the September 9 verification of `522cd1d` below remains historical. Local preview at http://127.0.0.1:5173 was opened in the user's browser after verification. Use `npm run dev` to reopen later. |
| Sources actually inspected | Eight dated Street View panoramas: six April 2026 views around the intersection, two closer west/Yubu views from September 2024. A nearest November 2017 selection was rejected for current claims. [Observation record](model-source/first-seventh-street-view-08.json) preserves public links, IDs, camera angles and uncertainty. |
| Changes | Tile Bar canopy/return; current Monkey/Hen signs; E7 return, displays and painted panels; Burger red blade/dining enclosure; E Smoke brick/window layout; Yubu shutter hardware; Saifee service windows, two red-brown escapes and complete side hardware/plant frontage; open escape grating, window hardware, utility lids, signals and red tactile paving. [Pass 08 report](model-source/FIRST-SEVENTH-PASS-08.md). |
| Preservation | Only `block-5-1`, `block-5-2`, `edge-south` rebuilt. Twenty-four protected files match the earlier source byte for byte: First & 10th model/recipe/photos, viewer/vehicle/glass/spatial/shared-texture code and eleven other GLBs. All 615 mapped footprints/frontages/heights, roads and bounds preserved. [Evidence](model-source/first-seventh-08-preservation.json). |
| Final technical checks | `npm run verify` passed after final review metadata: 26 modules, 2,308 road samples, collision/navigation/export signatures, exact street materials and spatial batching. Clean prepare/compile reproduced 615 buildings and all three audits. Final GLBs match recorded performance hashes. `git diff --check` passed; 251 relative links across all 19 Markdown files resolved before this final checkpoint refresh. |
| Browser and visual review | [26 final views and controls/recovery](model-source/first-seventh-08-final-review.json), [11 reference-position views](model-source/first-seventh-08-matched-review.json), [37 final game screenshots](docs/images/first-seventh-08/). Start/driving/resets, quality persistence, idle wake, deferred-core retry and startup fallback/Retry passed. Expected injected-failure warning only; no unexpected errors/failed requests. Final inspection confirmed Burger glazing is uncovered and mortar pattern retained. Matching GPS/angles with estimated 2.50 m camera height does not eliminate observed framing/proportion differences. |
| Performance | Chrome 152 / Apple M1, 1440×1000 DPR 1: Saifee Detailed 48.9 FPS, Faster 60.0, northbound driving stress 26.8, First & 10th SE 27.8; 38.86 MiB initial reconstruction transfer; zero idle scene redraws. Total neighborhood models 52.40 MiB / 9,435,099 triangles (+0.49%), maximum 63 material batches. Matte vertex-color batching and surface mortar bands limit the added cost. [Conditions and limits](model-source/first-seventh-08-performance.json). |
| Credentials and images | API key stays in ignored, owner-only root `.env`, outside served `dist/`. Repository scan found no key in 302 eligible files. All 25 temporary Google reference JPEGs were deleted after inspection; no Google pixels enter game assets or tracked screenshots. Access helpers print no keys or authenticated URLs. |
| Remaining quality limits | Exact murals/logos/font outlines, unreadable print, measured dimensions, complete occluded Yubu/Burger arrangements, interiors and some movable objects remain estimates. Lighting, vehicles and vegetation remain stylized. The gap report is not a claim those details are fixed or that the user’s 1:1 standard has been reached. |
| Next concrete step | Have the user review local chapter 08 with the four corner buttons and compare the recorded reference angles. Continue from their specific feedback on this corner; do not treat this checkpoint as visual acceptance, a deployment instruction or permission to expand the map. |

The current recipes are [first_seventh_refinement.py](model-source/first_seventh_refinement.py), [first_seventh_kit.py](model-source/first_seventh_kit.py), [first-and-seventh.json](model-source/first-and-seventh.json) and [storefront-details.json](model-source/storefront-details.json). The new refinement file participates only in affected corner export signatures. Before recompiling, note the inherited compiler behavior documented in the report: it resets three older non-corner `renderHeight` values (`241822303`, `241829661`, `248142640`); preserve their existing exported heights if their models are not rebuilt. Do not compile during a Blender export. All final exports and source checks are already complete.

## Previous checkpoint — 2026-09-09-03 (speed optimization published and verified)

Recorded **2026-09-09T01:19:25-04:00**. The user accepted **First & 7th revision 06 as the minimum completion standard**, authorized speed optimization and a push to main, and requested updated Markdown. The speed pass is implemented, pushed and verified live at **https://borough-drive.vercel.app**. The next discussion is an economical, repeatable street/block process for the existing East Village and eventually Manhattan/NYC. **No expansion or background jobs should start automatically.**

| Item | Checkpoint state |
| --- | --- |
| Exact gameplay/source commit | `522cd1de77b72186dc98e06a3b9f6ce4db761d8b` — Speed up neighborhood loading and rendering while preserving corner detail |
| Accepted visual baseline | `0f40856c9c576d1648ddae9c6e76bb550a98b42a`, First & 7th revision 06; five buildings, nine elevations and eight shop treatments. Accepted by the user September 9 as the minimum standard. |
| Checkpoint documentation | Publication records follow the source in a separate documentation commit. Identify that commit with `git log -- CODEX-HANDOFF.md SESSION-LOG.md`; do not amend to insert its own hash. No unfinished code/model export. |
| Implemented performance work | Exact street-material bootstrap; deferred First & 10th loading with retry; shared surface downloads/decodes; lossless spatial batches; thin clear glass with physical reflections; no idle scene redraws; frustum-culling of local lights; lazy fallback photo; current vendored HDR loader. |
| Preserved quality/data | Every original GLB, both accepted corner recipes, surface maps, six licensed core photographs and vehicle code byte-identical to revision 06. All manifest data identical except acceptance/scope metadata. Detailed remains default with AO/four samples. Tiny volumetric glass refraction is approximated by a thin pane. |
| Current inventory | 615 mapped objects; 411 photo-observed non-core records; 214 supported names, including 204 non-core; 27 individual non-core shop designs, 177 supported non-core names still estimated. 141 objects with a mapped frontage lack an individual photo record. |
| Local and remote | Resumed from clean `e86a480`, two unpublished commits ahead of `a44257a`. Fetched and preserved that work. Fast-forward push `a44257a..522cd1d` to the existing `rayidali/borough-drive` main succeeded. Final publication records are a following documentation-only commit. |
| Deployment | Vercel reported success for source `522cd1d`: [deployment](https://vercel.com/rayidalis-projects/borough-drive/6CxayAae3s7cYSh1cHWNHUy7YA18). All 20 game files changed since published pass 05 matched source byte for byte over HTTPS. Eight additional preserved assets also matched: core, six photographs and Draco WASM. [Publication evidence](model-source/publication-review-2026-09-09.json). |
| Verification | Local `npm run verify`; 26 modules, 2,308 road samples, navigation/vehicle/collision/source signatures; exact street-material subset; lossless triangle/UV/normal/transform/bounds tests. Clean prepare/compile reproduced source fields and three audits. [GitHub Actions](https://github.com/rayidali/borough-drive/actions/runs/34314169298) passed on Node.js 22 and 24. 212 relative Markdown links checked before publication. |
| Browser review | 26 final views plus two travel captures. Start/northbound driving/both resets/core/corner controls/quality persistence/idle wake-up passed. Injected deferred-core failure retried automatically; startup failure showed its lazy photograph and Retry restored play. No unexpected errors. Live browser smoke passed at the production URL: all five starting sections loaded, detailed Saifee view rendered, zero errors or failed requests. |
| Measured performance | Chrome 152 / Apple M1, 1440×1000 DPR 1: Saifee Detailed 25.2→50.2 fps; core SE 17.9→28.6; northbound streaming stress 20.7→28.1; Faster 56.5→60.0. Initial reconstruction transfer 89.61→38.24 MiB (about 57% less), shared image requests 42→9. Settled scene redraws fall to zero. Short local tests, not a universal device/route guarantee. |
| Remaining limits | Dense views/driving still below 60 fps on this device. New shader compilation/retina viewports can cost more. Photograph dates vary; Google panorama access, surveyed dimensions, unseen surfaces and precise artwork remain unresolved. Acceptance is a quality decision, not survey certification. |
| Next concrete step | Review the published speed improvements on the user's device, then discuss the economical block/street workflow. Use the accepted benchmark, source provenance, existing coverage inventory and per-device performance checks. Do not start new geographic work, paid imagery services or unattended batches without that later instruction. |

Read [Performance pass 07](model-source/PERFORMANCE-PASS-07.md), [before/after measurements and asset hashes](model-source/performance-review-2026-09-09/), [accepted corner report](model-source/FIRST-SEVENTH-PASS-06.md) and [source observations](model-source/first-and-seventh.json). All 18 project Markdown documents were refreshed; 28 final local PNGs and one live-site capture are tracked in `docs/images/performance-07/`. No Blender rebuild, engine migration, Firecrawl setup, API key or new photographic source was needed. The performance report records official Street View access options and extraction/derived-content restrictions for the later discussion. Credentials remain local.

### Photographic sources and durable records

| Purpose | Actual sources and where to resume |
| --- | --- |
| First & 10th architecture/photos | Wikimedia Commons/Flickr: Eden, Janine and Jim; Kidfly182. JLL full-building photographs are observation only. Exact dates, URLs, licenses and bundled files: [references.json](dist/reconstruction/references.json) and [core facade notes](model-source/FACADE-NOTES.md). |
| Individually modeled core shops | Official business sites including Nishaan and Gelatoville; dated EV Grieve and theater reporting. [Core storefront notes](model-source/CURRENT-STOREFRONTS.md) record those observations. |
| Wider neighborhood/storefronts | Village Preservation's Building Blocks archive, NYC landmark records, official business photographs (including Tile Bar), EV Grieve, Forgotten New York/Kevin Walsh, and separately credited architecture sources. [Storefront schedule](model-source/storefront-details.json), [facade audit](model-source/neighborhood-facade-audit.json), and [research records](model-source/neighborhood-references/) retain evidence and uncertainty. |
| Geography and materials | Included OSM snapshot and NYC OTI/PLUTO records anchor mapped geometry. Poly Haven surface maps are generic, not scans of these actual buildings. [Credits](dist/reconstruction/ASSET-CREDITS.md) and [third-party notices](THIRD-PARTY-NOTICES.md) preserve terms. |

Most new reference photographs were inspected to guide authored geometry; they are not copied onto every facade. Six licensed core photographs are bundled; selected core photo patches retain their original attribution. Other research images and historical temporary filenames may not exist locally: resume from the tracked source URLs, not old download folders. No image-generation guesses stand in for real reference photographs.

### Resume a session

1. Read this checkpoint, the latest [session log entry](SESSION-LOG.md), [START-HERE.md](START-HERE.md), and [neighborhood notes](model-source/NEIGHBORHOOD-NOTES.md). For historical context, read [pass 05](model-source/STOREFRONT-PASS-05.md), the [coverage inventory](model-source/digital-twin-coverage.json) and the historical [accuracy audit](model-source/ACCURACY-PASS-04.md).
2. Inspect the actual checkout without discarding changes:

   ```sh
   git status --short --branch
   git log -5 --oneline
   git log -1 --format="%h %cI %s" -- CODEX-HANDOFF.md SESSION-LOG.md
   ```

3. If remote state matters, fetch `origin` and compare it with the checkout before changing branches or pulling. Local `origin/main` can be stale. Record any uncommitted files or unpublished commits before continuing.
4. Use `npm run dev` to open the game locally when needed. A previous session's server, browser, authentication, or `/tmp` files may no longer exist. All required game assets and reproducible source are in the repository; no Blender rebuild is needed to resume.
5. Preserve the accepted First & 7th minimum standard and follow the user's current scope/publishing instructions. The wider process awaits the next planning discussion. For an authorized game publication, include all required models, textures, manifests, runtime modules and source records. Confirm deployment success and compare live content with the intended commit before describing it as live.
6. Update this checkpoint and add a dated log entry at the next meaningful milestone or handoff. Record partial work and failures when the task is incomplete. Do not present an old test or browser review as a new check.

## Historical export baseline

- Exported September 6, 2026 from the latest published ten-block version, version 4.
- Original source commit: `67c99489185417c9de9e6a7292de5329df00c82a`.
- At export, runtime and model files matched that baseline. Export-only additions were the local server, package scripts, setup instructions, and this handoff. The original hosting identity and Git metadata were omitted.
- The game can run entirely from the included static assets. No backend, Google API key, or live map service is required.

## Open-source repository preparation — September 6, 2026

- The owner authorized a public repository at `https://github.com/rayidali/borough-drive` after local GitHub authentication confirmed `rayidali`.
- Original code, documentation, and independently authored artistic contributions use MIT. Third-party and geographic data terms remain in `THIRD-PARTY-NOTICES.md` and the original asset credits.
- The repository adds a visual README using the existing Blender renders, contributor documentation, issue/PR templates, and GitHub Actions verification on Node.js 22 and 24.
- Personal setup instructions were replaced with public contributor instructions. Runtime code, geometry, textures, and reference photographs were preserved.
- The initial publishing request covered the source repository. Later requests authorized the Vercel deployment and complete revision 04 publication recorded in the current checkpoint. The geographic scope remains the existing map.

## User's objective and feedback

The public game is connected to this repository's `main` branch. The repository-root `vercel.json` serves `dist/` directly and skips installation and building. Historical gameplay passes include revision 04 in `835ea6c` and pass 05 in `63757cb`. First & 7th revision 06 is accepted; performance pass 07 improves the runtime. Exact current source/push/deployment states are recorded above.

Rayid wants to build a driving game in a recognizable digital twin of New York City, eventually Manhattan and then NYC. The immediate approved scope is ten connected East Village blocks around First Avenue and East 10th Street: East 7th to East 12th, Second Avenue to Avenue A.

The user knows this neighborhood and rejected generic buildings with real business names. Google photorealistic tiles were closer but still did not meet the desired street-level quality. The accepted approach is authored 3D geometry based on actual map footprints and photographic observations. First & 10th is the most individually detailed core and should remain the quality anchor.

Desired atmosphere: cozy, aesthetic, warm, calm exploration, with the relaxed feeling of slowroads.io but substantially stronger geographic and architectural fidelity. Prioritize recognizable facades, building proportions, rooflines, fire escapes, storefront openings, cornices, windows, signage, sidewalk details, materials, and good driving feel. Preserve real geometry while improving lighting and presentation.

Do not mistake a verified business name for a verified facade. Storefront availability and architectural reference dates must remain separately documented. Unknown occupants should stay unnamed; historical signs must remain marked as historical.

## What currently exists

- Ten complete blocks plus four boundary sections, covering three avenues and six cross streets.
- 615 building objects including five restored municipal footprints, annexes and boundary context; this is not 615 independently verified facades.
- 411 additional building records with photographic observations, from 459 combined source records. Coverage varies from storefront observations to fuller facades.
- 620 municipal footprint matches, PLUTO 26v2 parcel attributes and 214 independently supported named place records, including 204 outside the core; September 8 removed two duplicate identities. The audit retains excluded and unresolved entries.
- Blender-generated Draco GLBs with shared textures, streamed by distance.
- A highly detailed preserved First & 10th core, map travel, walk/drive modes, collision, fixed-step vehicle simulation, street props, warm lighting, and source/accuracy information.
- Business baseline checks dated September 6, 2026, two identity corrections/exterior review dated September 8, and OSM snapshot dated September 5. Image dates vary. Twenty-seven non-core shops have explicit exterior designs; remaining named shop designs are estimated.

This remains a reconstruction prototype with uneven detail. There are 141 non-core objects with mapped frontages but no individual photographic observation. Secondary elevations, some road widths, furniture positions, trees, interiors and unseen geometry remain estimated. Many archive photographs have 2012 upload dates and unknown capture dates. Do not describe it as a surveyed, fully current digital twin. Pass 05 browser checks are linked above; older validation below remains historical.

## Where to work

| Path | Purpose |
| --- | --- |
| `dist/index.html` | Active ten-block game page |
| `dist/reconstruction/viewer.js` and `viewer.css` | Main rendering, interaction, and interface |
| `dist/reconstruction/vehicle.js` | Fixed-step vehicle dynamics |
| `dist/reconstruction/neighborhood-world.js` | Collision, street geometry, navigation |
| `dist/reconstruction/neighborhood-render.js` | Section streaming and instanced props |
| `dist/reconstruction/neighborhood-map.js` | Minimap and travel map |
| `dist/reconstruction/neighborhood.json` | Geometry, facade data, visual sections, prop placements |
| `model-source/neighborhood-observations.json` | Editable photographic observation schedule |
| `model-source/neighborhood-facade-audit.json` | Revision 04's 376 individually inspected facade observations |
| `model-source/geography-corrections.json` | Municipal matches, five restored footprints and address corrections |
| `model-source/business-corrections.json` | Reviewed tenant overrides and contrary evidence |
| `model-source/neighborhood-business-audit.json` | Generated supported/unresolved business inventory |
| `model-source/street-facilities.json` | Dated street facility evidence and explicit dimension estimates |
| `model-source/neighborhood-references/` | Detailed architectural and occupancy research records |
| `model-source/build_intersection.py` | Blender recipe for the detailed core |
| `model-source/build_neighborhood.py` | Blender recipe for neighborhood sections |
| `model-source/storefront-details.json` | Explicit shop designs, per-street elevations and dated photographic provenance |
| `model-source/storefront_detail_kit.py` | Blender components for signs, openings, awnings and observed furniture |
| `model-source/digital-twin-coverage.json` | Map-wide inventory of remaining frontage evidence/design gaps |
| `model-source/storefront-review-2026-09-08.json` | Local browser poses, graphics settings, asset hashes and validation |
| `model-source/neighborhood_landmarks.py` | Landmark massing and facade forms |
| `scripts/prepare-neighborhood.py` | Derive mapped geometry from included OSM snapshot |
| `scripts/compile-neighborhood-details.py` | Apply observations and deterministic details |
| `dist/vendor/` | Included Three.js r180 and Draco runtime, with notices |
| `dist/prototype.html` | Earlier generic prototype, preserved for history; not the active game |

## Running and validation

Use Node.js 22 or newer. `npm run dev` serves only `dist/` at http://127.0.0.1:5173. It uses built-in Node modules and needs no dependency installation. `npm run verify` runs the original meaningful vehicle, road, map-spawn, GLB, texture, and module checks.

The original September 6 exported copy was checked with Node.js 24.19.0: both verification scripts passed, including 2,308 road samples, valid map spawns, boundary driving, braking/reverse/collision, and frame-rate independence. A local HTTP smoke check passed for the page, JavaScript, Draco WebAssembly, core and section GLBs, and map JSON. This was a source/asset/server check, not a browser gameplay or visual performance test.

Use Blender 4.3 or newer only when mesh regeneration is needed. Read `model-source/NEIGHBORHOOD-NOTES.md` for the rebuild order. The preparation script resets derived facade and prop data, so the detail compiler must follow it. Rebuild only the affected neighborhood section where possible, for example:

```sh
blender -b -t 6 --python-exit-code 1 --python model-source/build_neighborhood.py -- --tile block-3-2
```

For optional standalone renders, specify a portable output location:

```sh
blender -b -t 6 --python model-source/render_neighborhood.py -- --output renders/neighborhood
```

Existing model exports are included; do not make Blender installation a prerequisite to opening or editing the viewer. Do not regenerate every model just to inspect the project.

## Later map-wide work — bounded pilot prepared

First & 7th revision 06 remains the accepted minimum; pass 08 is the current working visual reference and pass 09 improves browser presentation. The [block workflow](model-source/BLOCK-WORKFLOW.md) now supplies the finite inventory, small pilot plan, source registry and offline proposal validation. Its next step is authorized full/oblique imagery and a three-facade cheaper-model evaluation, followed by rendered review before adjacent-block work. Manhattan/NYC remain longer-term objectives. A working local Google key exists but does not establish permission for the new automated workflow. No engine migration, Firecrawl service, paid worker or citywide expansion has started.

## Previous local work — neighborhood detail revision 03

Rayid requested that the entire existing map reach and exceed the core's detail level. All fourteen existing sections were rebuilt using the shared recessed-facade system in `model-source/neighborhood_detail_kit.py`, with source-specific controls in `neighborhood-detail-schedule.json`. The pass covers 587 non-core building objects, 547 with mapped street frontages, and adds 235 instanced street objects. It also fixes stretched road/pavement UVs and enables multisample antialiasing for the postprocessing targets.

That pass preserved the core GLB/recipe, six photographic source files, mapped footprints, roads, navigation bounds, vehicle dynamics and collision module. Its photographic coverage remained 77 additional observed building records. The user rejected the insufficient geographic and storefront fidelity; this led to revision 04. See `model-source/DETAIL-PASS-03.md` for the historical pass report.

## Historical foundation — accuracy revision 04

The following summarizes revision 04 at `835ea6c`; its counts and validation predate pass 05. Read `model-source/ACCURACY-PASS-04.md` for that historical audit. Every municipal footprint in the existing extent has an explicit match. Five missing substantial buildings are restored: 73–75 East 7th, 256–258 East 10th, 323–325 East 12th, 195–197 Avenue A and East Side Community School at 420 East 12th. Only the new footprints are aligned to the accepted OSM frame. Existing core geometry remains unchanged.

The facade audit brings photographic coverage to 410 non-core building objects. Individual controls now include bay positions, story/basement separation, actual curved openings, recorded cornices and fire escapes, ground use and special entries. Blue & Gold has a dedicated low frontage profile; 45 East 7th uses the completed replacement building rather than the archive's vacant-site condition. The school's H-shaped outline, grouped glazing, recessed entry and internal courtyard are modeled separately from PS 19.

The compiler now processes all 369 mapped retail/food/bar/bank candidates, website address evidence, city inspection records and reviewed corrections. Of 379 total audit entries, 212 support a name and 167 are excluded or unresolved. Businesses attach to individual street frontages and estimated shop units. The data inventory is not a count of independently photographed storefront designs. Ferns, superseded Pasta de Pasta at 192 First, Blank Street and historical Beron each retain contrary/status evidence. Search in the travel map takes a name or address to its corresponding street.

Cross-street bicycle facilities, Avenue A lanes and Second Avenue's cycle/bus treatment are recorded separately from illustrative street dressing. Existing road widths and vehicle simulation are preserved; protected-side illustrative parking on East 12th is omitted to retain a clear driveable lane within those approximate widths. Building collision and distant silhouettes now preserve courtyard holes. Material variants prevent differing same-name core/neighborhood colors from being incorrectly shared.

All fourteen neighborhood GLBs were rebuilt, followed by affected-section corrections from browser review. The ordinary compiler and source preparation need no external packages or network calls. A final clean preparation/compilation in an isolated directory reproduced the working facade, geometry, business and prop data. The local server uses `npm run dev` at http://127.0.0.1:5173. Production deploys from `main` using the included static assets.

Final validation: `npm run verify` passes on Node.js 24.19.0, including 2,308 road samples, vehicle/navigation invariants, model/texture references, municipal joins, courtyard collision and bounded business partitions. Section exports total 48.67 MiB and 8,761,042 triangles, with at most 46 material batches per section. All 610 original footprints, road centers/widths/directions and driving bounds are unchanged. Core model/recipe and six licensed reference files were byte-identical to the revision 04 comparison baseline.

Actual browser views were inspected across all fourteen sections, then rechecked after correcting the school entrance and over-wide corner shops on 21 buildings. Model/texture requests succeeded and no uncaught exceptions occurred; existing RGBELoader deprecation warnings remain. Name/address searches and invalid-input handling passed in the real interface. Two sequential 180-frame stationary samples on Apple M1 / ANGLE Metal at 1440 × 1000 recorded 30.9 fps near Blue & Gold and 60.0 fps near the school. Default adaptive quality was enabled; this is not a sustained or fixed-quality driving benchmark. See `model-source/accuracy-review-2026-09-06.json` for conditions and `docs/images/` for actual browser captures.
